[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "sceneops-core-events"
version = "0.1.0"
description = "Versioned event reference and compatibility rules for SceneOps Forge"
requires-python = ">=3.9"

[tool.hatch.build.targets.wheel]
packages = ["src/sceneops_core_events"]
