# Git 制作分支与策划修改提案

后续增量已接通卡片内受控源码写入，原文“没有开发执行”的限制由 `CARD_CODE_DEVELOPMENT.md` 更新；自动运行、合并和demo组装仍未接入。

## 当前操作

1. 新文件夹项目创建独立 Git 仓库，默认分支 `codex/integration`，不继承外层应用仓库。
2. 确认策划版本时，生成真实提交和 `vN` 标签；不会把用户暂存的其他文件带进提交。
3. 制作卡片单排从左向右展示，可横向滚动。点击卡片创建 `codex/card-<card_id>` 和独立 worktree，再次点击核对并复用原分支。
4. 主对话显示所选卡片、分支和工作目录；回到项目只清除对话上下文，不切换或破坏工作目录中的修改。
5. 对话提出大纲/卡片修改时生成前后对比。确认采用只修改工作稿，正式 `v2` 仍需另行确认。

旧文件夹项目不会在启动、读取或自动保存时初始化 Git。在版本记录点击「将已确认版本纳入 Git」，或明确进入卡片时，才补建已有正式版本的提交。已有标签、分支或工作目录没有应用登记时拒绝自动接管，不覆盖、不强推、不清理。

## 数据与目录

项目根目录是集成 checkout；worktree 位于应用数据目录 `card-worktrees/<project_id>/<card_id>`，避免嵌套进根仓库。首次创建分支放置 `.sceneops/card-brief.json`，保留卡片身份和最初工作说明。

Git 原生对象 ID 是提交引用，没有另加内容哈希。版本发布使用临时提交 index、真实 index 锁和 Git 引用事务；只把已确认快照更新进暂存区，保留不相关暂存内容。中断的登记版本可以核对后恢复 index 发布；不删除未知 Git 锁。

修改提案保存 before/after 结构与来源 revision。采用时再次核对大纲和卡片未被改动；普通聊天、输入保存不算设计冲突。移除卡片不会删除已有分支和文件。

## 已运行验证

- 4 项临时真实 Git 烟测：版本与 worktree 复用、未知分支拒绝、暂存冲突保留、中断后的 index 恢复。
- 5 项策划集成烟测：包含修改提案采用、正式版本不自动增加、真实卡片分支创建/复用、旧非法卡片 ID 不锁死后续编辑及原有并发/恢复检查。模型使用 fixture，无真实推理。
- 3 项前端组件烟测：Markdown、逐题选项、修改前后预览和明确采用。
- 浏览器只读检查：16 张现有卡片位于单一横向 flex 行，容器宽749px、内容宽4470px；没有点击用户卡片或替用户初始化旧项目 Git。
- API 健康检查通过。本轮没有运行游戏、Unity、Blender、构建、发布或模型写代码。

## Limitations

当前交付分支/worktree 准备和主对话上下文，并非所选分支内的自动代码执行引擎。自动合并、冲突解决和 demo 组装未接入；不自动 rebase 旧 worktree。版本按钮当前只提交确认的策划快照，不自动提交其他资产/代码，仍可使用普通 Git 工具管理这些文件。新模型修改提案的真实质量待用户发送验证。
