[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sceneops-blender-adapter"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["pydantic>=2.8,<3"]

[tool.setuptools.packages.find]
where = ["src"]
