[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[project]
name = "sceneops-ai-provider"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx==0.28.1", "sceneops-codebuddy-cli==0.1.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/sceneops_ai_provider"]
