# Warehouse Escape sample

Warehouse Escape 使用同一 Runtime、身份、遥测和构建路径，生成 switch、door 和 exit。它只改变项目数据与场景配置，不复制或修改 SceneOps 平台实现。

运行 `modules/engine-unity/scripts/run_unity_smoke.py` 可在具备有效 Unity 2022.3 许可证的宿主上生成真实 standalone build；否则结果必须保持 `blocked`。
