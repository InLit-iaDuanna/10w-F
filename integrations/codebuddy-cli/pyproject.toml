[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"
[project]
name = "sceneops-codebuddy-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["jsonschema==4.26.0"]
[tool.hatch.build.targets.wheel]
packages = ["src/sceneops_codebuddy"]
