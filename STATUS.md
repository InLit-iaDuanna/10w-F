# 当前状态：新制作已切换为确认简报后的原生 CLI 主线

## 2026-09-09：原生 CLI 制作与实际回流验收

已接入简报确认、Codex／CodeBuddy 持久会话、独立权限模式、任务绑定 MCP、五类制作技能、真实源码登记与共享引用更新。CodeBuddy 当前模型完成风暴灯塔岛与准确会话提速；实际键盘通关／重开、宽窄窗口、工作台实例与共享资产修改后重建通过。Codex 仅本地适配验证。43 项后端、11 项前端定向回归及工作台构建通过；新增源码重命名和构建失败保留候选另有回归。限制及不可混同的失败证据见 [验收报告](NATIVE_CLI_PRODUCTION_ACCEPTANCE.md)。未提交、合并、发布。

## 2026-09-08：首次环境配置

已接入选择 CLI、固定版本用户目录安装、macOS 终端登录和连接验证的三步向导；支持跳过与再次进入，安装过程可后台继续。Codex 不兼容版本可单独适配，保留全局安装。最小模拟安装、版本修复、前端失败重试主路径及新增文件定向类型检查通过；浏览器核对首次页、安装页和登录页。未进行真实安装/登录/模型调用或完整套件。见 [环境配置说明](modules/conversation-home/docs/environment-setup.md)。

## 2026-09-08：卡片源码精修

卡片内已接入「架构与源码」：按真实目录选择分区／文件、手动编辑、限定文件范围的受控 AI 修改。写入权限由持久授权和实际源码工具检查；不是仅提示词约束。定向后端与前端烟测通过，未跑真实模型和整条构建试玩。见 [交付说明](modules/ai-agent-runtime/docs/CARD_SOURCE_EDITOR.md)。

## 2026-09-07：新建项目身份与工程基线

新建项目先写 `.sceneops/project.json`、验证独立 Git，再登记 SQLite；中断后可显式恢复。架构 scaffold 现在以选择性提交进入 `codex/integration`，不安装依赖、不包含 lockfile、依赖目录或用户额外文件。新卡从当前有效 integration HEAD 创建，旧卡保持原 base；移动与复制项目使用明确分流，副本在已有工程采用完成前不能进入开发旅程。定向后端和 UI 烟测通过；全项目类型检查仍被已有诊断阻断。见 [身份与基线里程碑](GAME_PROJECT_IDENTITY_BASELINE_MILESTONE.md)。

## 2026-09-06：游戏代码架构选择与真实工程

第一次生成代码前可手动选择对象／组件式或 ECS · Miniplex，也可由当前 AI 提供方根据策划推荐；确认后创建真实 Vite + TypeScript + Three.js 工程。技术方案进入制作卡、Git worktree 和 Agent 任务上下文，旧项目不从 Three.js 猜架构、不覆盖源码。两类样例的类型检查、构建和浏览器输入通过；真实 `gpt-5.6-sol` Agent 在 ECS worktree 内只修改输入系统增加 Shift 冲刺，随后构建通过。见 [里程碑交付](GAME_CODE_ARCHITECTURE_MILESTONE.md)。

## 2026-09-06：分支内受控源码开发

选中卡片后，主对话可准备开发授权；CodeBuddy/Codex/OAI通过原Harness读写已登记worktree源码，展示差异并回读。7项相关烟测通过，未调用真实模型、执行源码或改变用户项目。任务完成只表示源码写入，不表示编译/游戏验收。见 [卡片源码开发](CARD_CODE_DEVELOPMENT.md)。

## 2026-09-06：真正的 Git 卡片分支

文件夹项目初始化独立 Git；正式策划版本生成提交/标签；横向卡片可创建/复用对应分支与 worktree，主对话携带选中上下文。对话修改大纲/卡片先展示提案，确认后采用。12 项相关烟测通过，未改写用户现有项目的 Git。执行边界和待完成项见 [Git 卡片分支](GIT_CARD_BRANCHES.md)。

## 2026-09-06：对话渲染与流式交互

策划正文支持 Markdown；新 grill-me 对齐使用一次一题的结构化选项卡，制作清单恢复可见卡片；输入框为单层紧凑样式。新增流式回调及 SSE，只展示真实文本/提供方思考事件，结构化结果不输出裸 JSON。10 项相关 fixture/组件烟测通过，未新增真实推理；旧历史不改写。见 `PLANNING_JOURNEY_STAGE1.md`。

## 2026-09-06：单人协作策划阶段一

文件夹项目、单主对话聊 idea、显式 grill-me、大纲编辑和 v1、Three.js 确认、可编辑制作卡片已接入。目录与策划的定向 fixture 烟测和页面入口/草稿检查见 [阶段一交付](PLANNING_JOURNEY_STAGE1.md)。这不是 GLB 编辑或 Three.js 游戏执行已完成；真实模型质量与后续制作仍未验收。

## 2026-09-06：优先制作，自动游测默认关闭

用户要求先跑通制作流程，AI 玩游戏暂缓。新任务默认不授权自动 Play/游测；原型交付检查真实场景、版本、保存、编译与控制台，通过后显示待人工审阅/试玩，不冒充玩法验收。本次两项生产交付合同烟测通过，未重新运行游戏。此前真实方块生存原型 P0/P1 的执行记录、GLM 修复结果和限制见 [方块打僵尸验证记录](MC_ZOMBIE_LIVE_VERIFICATION.md)。下方“未运行游戏”等描述是历史阶段记录，不代表最新验证范围。

## 最新增量：生产快照、事件流与产物版本

工作台默认改为状态/产物区；主对话独占聊天、模型与授权入口。项目级 SQLite 事件与 SSE、多任务互斥、产物不可变版本、真实行动投影及不抢焦点的结果组打开已实现。只运行定向夹具烟测和启动/页面空态检查，未运行游戏、构建、渲染或游测。完整四阶段计划尚未完成，特别是受控 DCC 连续任务、通用步骤图与后段生产执行器。详见 [本次分段交付与剩余项](SINGLE_AGENT_WORKBENCH_HANDOFF.md)。以下真实闭环均为此前独立验收记录，不是本次全链通过。

## 最新增量：Codex CLI 与单一 Agent 对话

已增加 `codexcli`，保留 CodeBuddy/OAI，分别保存模型。统一一个输入框、权限选择、流内任务卡；业务工作台默认 Agent，手动设置折叠。用户随后批准 Codex 完全权限，新增显式授权入口，真实 `gpt-5.6-sol / low` 独立文件创建和读回烟测通过；其他生产处理器未额外验收。见 `CODEX_PROVIDER_HANDOFF.md`。

## 最新：一次任务授权，真实 Blender → Unity

2026-09-05：CodeBuddy `glm-5.3-flash` 经应用注册能力执行，4/8 次模型调用完成独立箱体任务。Blender 5.1.2 回读 `[1,2,3]` 米，Unity 2022.3.62f3c1 回读 `[1,3,2]` 米，同一 asset_id/sceneops_id，Unity console errors 为空。真实处理过 Unity 插件未加载阻断，修复后使用原 grant/request ID 恢复，未重复创建对象。聊天/Agent 模式、授权卡、任务记录、取消/连接恢复、生产树进度与 SQLite 已组合进统一应用。验证和限制见 `AGENT_LIVE_VERIFICATION.md`。

以下为历史增量记录；其中“未连接/未验证”描述对应旧交付，不覆盖最新基础资产闭环。其他生产能力、生产构建、游戏案例、渲染与 playtest 没有新增完成声明。

## 最新：嵌套区域、中性灰与生产节点树

按用户补充，统一应用由全局边栏改为当前区域内的嵌套原生拆分，旧标签迁移保留，消除空中心带。全局底色与聊天一致，工具库改为生产流程节点树。真实鼠标验证局部拆分不影响兄弟区域、内层收起及搜索/原位切换。Blender/Unity 仍未真实连接，需要确认目标工程后补齐接入；安装探测与真实适配器缺口已核实。见 `NESTED_REGION_VERIFICATION.md`。

## 追加：四边稳定性、即时聊天与诊断

取消固定拉伸上限，原生分界线拖回边缘收起；修正相邻边栏尺寸反馈循环、满屏时禁用分界线遮挡，以及全关闭后聊天落入隐藏边栏。发送立即显示临时消息，允许继续写草稿。右上角「诊断」保存最近 200 条本机摘要。定向测试、真实鼠标与显式 Mock 请求验证见 [本轮记录](INTERACTION_POLISH_VERIFICATION.md)。历史浏览器硬崩溃无堆栈，类型债及生产链未验证项仍保留。

## 追加：聊天视觉结构再次重做

按用户要求聚焦 Codex 式聊天外观：移除欢迎介绍/建议卡和输入框内生产计划按钮，改为中性灰、单层圆角输入、底部「＋ / 模型 / 设置 / 发送」工具栏。生产计划放入对话选项，已保存上下文放入「＋」菜单。实际浏览器检查历史对话、上下文和设置弹窗；修复焦点滚动整个 Dockview 外壳的问题（外层 clip、编辑器内部滚动）。未发送 AI 或修改用户设置。并非桌面源码逐像素复刻，等待用户参考图以便进一步对齐。

## 追加：用户授权的 UI 功能回归

修复顶部入口遮挡、原生尺寸 API、Peek 幂等、折叠大小记忆和双击冲突；失败面板保留切换入口。聊天改为紧凑输入交互，角色入口修复 Zod 主版本预打包冲突。Shell 40、bridge 25、角色 11 项通过，13 入口浏览器原位空态通过；对话旧测试 40/42，类型检查仍 265 项诊断。详见 `UI_FUNCTIONAL_VERIFICATION.md`；没有新增真实 AI 或外部制作链验证。

## 追加：统一界面视觉重做

按用户允许调整旧视觉规范的要求，重做石墨灰/蓝色工作台外壳、紧凑对话与输入面板、模型设置弹窗、中文分组工具选择器和生产计划空态。保留原位承载、四边拉手、上下文和 AI 接口。仅做独立空数据目录的启动与 UI 烟测；未发起 AI、保存设置或运行业务。说明见 `UI_REFRESH_SMOKE.md`。

## 追加：拉出区域原位选择与承载

按用户明确的交互要求，四边空区域统一显示功能选择器，默认在当前面板切换功能，位置、尺寸与上下文保留。修复原生空抽屉反馈覆盖用户拉出指令，以及先向中心创建面板的问题；选择已打开的单例保留既有实例。补充窄面板响应式排版。此次只做 UI 空态路径烟测，不调用 AI 或运行业务。结果见 `DRAWER_UI_SMOKE.md`。

## 追加：用户授权的 AI 真实连通修复

修复 CLI JSON 数组结果解析及本机原生结构化模式挂起依赖；保持全部工具禁用，采用应用严格 JSON Schema 校验。GLM 已实际完成聊天、模块建议、结构化计划与单专家分析；最终计划专项返回 completed，读回 5 条持久事件；4301 页面已显示并保存真实连通回复。12 项专项离线回归通过。GLM 复查有格式拒绝，HY4 有超时，不宣称稳定性完成。详情与失败记录见 `AI_LIVE_VERIFICATION.md`。

## 初始 V5 交付记录

当前用户要求详见 `HARNESS_MIGRATION_DECISIONS.md`。V5 内核由 Astra、Provider 由 Sol、统一 UI/UX 由 Terra 实现；主代理串联 AI 模块、生产计划界面和统一后端，另由 Astra 独立只读审查高风险边界。上一次整合提交 `34121bf` 及其记录保留。

V5 空态启动、健康接口、空项目、模型候选列表及目录注册已通过最小烟测：16 项能力、10 个产品角色、5 层模型配置。未运行测试套件、生产构建、业务操作、外部工具或真实 AI。不能据此声明完整 V5 或生产链完成。交付与未验证项见 `V5_HANDOFF.md`、`V5_SMOKE.md`、`PROTOTYPE_GAP_MATRIX.md`。V5 预览为 4301/8301、独立 `.local/v5-preview` 数据；未主动停止旧服务，收尾发现此前后台进程已结束后仅重启 V5 预览。

## 上一版整合结果

2026-09-05：11 个工作台历史已合入 `codex/integrated-workbench`。统一 `pnpm dev`、Web 4300/API 8300、原生工作台注册、SQLite 项目/草稿和统一 CodeBuddy 接口已实现。启动烟测确认健康、11 组注册、空项目、空聊天、模型目录及10个业务面板空态；未运行业务测试或案例。完整记录与已知限制见 `INTEGRATION_SMOKE.md`。真实 AI 尚未成功验证，可见页面的手动请求出现 503，代理未发起或重试。

本轮用户已批准将 11 个工作台合入主项目整合分支。默认空工作区，本地 SQLite 保存，AI 统一 CodeBuddy CLI。只授权一次启动/导入/空态烟测；禁止 demo 案例、业务操作测试、真实 AI 请求、完整测试及生产构建。原工作树与历史数据保留。

## 本轮写入所有权（已收尾）

- 主代理：apps/web、packages/core-ui、packages/api-client、前端共享 workspace 客户端、根依赖/锁文件/启动脚本、生成目录、根文档与最终启动烟测。
- ai_adapter：integrations/codebuddy-cli、modules/conversation-home（后端、统一聊天/建议前端与模块文档）、concept-lab/design-room 中已有 CodeBuddy 后端适配器的兼容委托。禁止修改主应用或项目存储。
- workspace_backend：modules/project-intake/backend、services/api、其余业务模块后端的统一组合/空态/按项目存储适配（不改 AI 适配器），后端 requirements。先发布公开接口供前端消费。
- workbench_editors：除 conversation-home 外的业务模块前端、apps/labs 中用于提取的前端组合入口，负责公开可嵌入编辑器、空态/项目上下文和草稿保存对接。禁止修改主 Shell、公共包、后端或根依赖。

所有代理不创建新任务、不用 Bridge、不提交、不启动服务或测试。主代理统一验证并提交。

## 先前 Shell 交付记录（不是本轮验证证据）

2026-09-05：整合原 01 `477e673`、02 `1a14081`、03 `1eca516`；公共骨架提交 `4f3416e` 已先完成。

当前交付：`apps/labs/shell` + `apps/web` 公开组合、真实 conversation-home、Dockview 四边抽屉/工具库/命令搜索、统一 pnpm workspace 和生成模块目录。`core-ui` 是旧 Shell UI 类型的唯一公共位置，Shell 保持转导；core kernel 未重写。

新增用户要求已接入：CodeBuddy CLI 文本 API（8310）及前端 15 模型选择，默认 MOCK；真实请求由用户发送时执行。本地 CLI 版本 2.144.0；登录、额度和模型推理未运行。

验证：启动和公共导入通过；唯一最小路径“对话提案→预览→确认→右侧工具库”通过；刷新启动恢复布局；本地模型目录 GET 和下拉框可用。许可证 console 提示为本地 Enterprise 评估水印的预期行为。

完整单元、集成、类型、构建、E2E、安全、性能、外部模型、Unity/Blender/渲染/AI playtest：not run / pending approval。未启动暂停的演示/审计/SDK/验收任务；未修改来源工作树、未自动合入主工作树、未部署。

详细操作、限制与验证记录见 `apps/labs/shell/README.md`。

2026-09-06 版本树：统一版本管理入口已接真实 Git 分叉/合并拓扑及策划进度，支持节点文件详情、分支高亮、拖动/缩放与 HEAD 定位。临时 Git 仓库 API 主路径和布局最小冒烟通过；未运行完整套件或浏览器交互验证。见 modules/version-collaboration/docs/unified-workbench.md。

## 2026-09-08：完整项目工具入口

工具库由四项扩展为八项，分为「策划与制作」「游戏内容」「试玩与版本」。新增策划与制作卡片、制作进度、架构与源码、游戏试玩；保留原有四项。策划面板读取大纲和卡片，进入卡片仍使用现有 Journey 命令；源码面板选择登记 worktree 并固定卡片上下文；进度面板按任务折叠详情；试玩复用已有工程状态、构建和预览操作，过期源码不会冒充当前试玩。

环境场景改成无套层边框的画布与可调整高度的资产栏。空场景提供内置资产和导入入口，资产区使用紧凑标题、计数与图标操作。场景对象与资产服务写入语义未修改。

验证：工具分组与搜索 5 项、源码草稿保护、卡片上下文固定、后端源码读取与限定写入最小烟测通过；改动入口定向类型检查。浏览器实际读取当前项目的源码、策划、运行记录与任务列表，并目视检查环境场景及资产空态。本轮没有发起模型生成、保存用户源码、运行构建或试玩。

## 2026-09-08 主对话制作

已接通不经制作卡片的讨论、单问澄清、权限选择和原生项目执行。检查、构建与试玩启动由既有运行器自动承接，权限与范围仍独立验证。定向结果见 [CHAT_FIRST_PRODUCTION.md](CHAT_FIRST_PRODUCTION.md)。

游戏执行系统提示已补齐按玩法选择的相机策略、世界/屏幕尺寸链路、DPR、宿主缩放与真实视觉检查要求。GLM-5.3 重跑后，宽/窄窗口和 DPR 1/2 显示及移动、收集交付、重开实测通过。见 [验证记录](CHAT_FIRST_PRODUCTION.md)。

## 2026-09-08：Agent 多平台导出

独立导出页面、持久任务、分平台重试、Agent 对话与本地打包适配器已接入。最小 macOS arm64 真实包构建及无服务器启动通过；安卓、Windows、Intel Mac 和完整游戏交互待验证。见 [精确证据与限制](PLAYABLE_EXPORTS_MILESTONE.md)。

## 2026-09-08：导出 Agent 技能

系统提示词与四个产品技能已完成并接入知识加载，格式检查与十项定向测试通过。参考成熟 GitHub 官方项目，保留来源与固定版本。导出页面接入原生电脑执行仍待独立任务 profile，不宣称已自动补 SDK。见 [技能交付](EXPORT_AGENT_SKILLS.md)。

## 经验记忆与分类（2026-09-08）

已加入经验服务、四条请求接入、26 条/51 处种子证据、自动学习与独立预算。经验页已重设计为经验库/学习动态/设置，支持六类主题、多主题归属、工具与状态筛选及人工修订。35 项后端定向检查、3 项组件检查、定向类型检查与生产构建通过；两轮隔离真实模型验证共 4 次调用。范围与局限见 [经验验收记录](docs/experience-validation.md)。未部署或发布。

## 2026-09-08：导出页原生命令执行已接通

专用 project-export-agent 复用授权与执行通道，导出页默认原生执行、支持讨论/取消/恢复。真实 CodeBuddy 从导出消息执行 node --version 并写文件，状态 succeeded、读回 v22.22.0。取代前文“原生导出待接”的限制；见 [接入与精确验证](NATIVE_EXPORT_AGENT.md)。


## 2026-09-09：中转 GPT 原生执行

完全访问下，OpenAI 兼容中转 GPT 使用 Codex CLI 原生执行，向保存服务的 Responses 接口发送请求。每次独立临时 HOME/CODEX_HOME；真实上游 Key 留在后端，CLI 使用固定目标与模型的临时本地凭据，Shell 不继承该凭据。默认不设总时限、内部模型请求与动作次数不限，可随时停止。

所有项目 Agent 直接显示实际文字、命令、文件与状态。旧 typed 任务保留历史和源码；在相同方向、工作区且无未决写入时，新原生授权可原子接管。未将旧任务改成已成功。

本机 Codex CLI 加本地 Responses 夹具完成真实命令写文件、事件输出与凭据/配置清理；前端原生路由、旧任务交接、对话静态渲染定向冒烟通过。没有调用用户的真实中转模型或构建游戏。组件测试运行器因已有 picomatch 错误未启动。细节见根目录 NATIVE_API_EXECUTION.md。

## 2026-09-09：初版整理为分项制作卡片

新增从现有原生制作工程整理大纲与源码绑定卡片的入口。草稿需审阅，原生卡片在同一工程与会话续改；旧卡片 Git 工作区路径保留。普通源码与托管模型身份不混用。

定向验证：策划生成→确认→进入卡片、不创建分支、源码丢失拒绝续改（1 项）；运行模块工作区核对与原生卡片完整回流（2 项，临时真实文件/数据库，模型与构建执行使用夹具）；前端续改路由（8 项）；策划工具交互（2 项）。类型检查在本次修改入口未报告错误，但其依赖仍有类型/模块解析诊断，因此不声明项目整体类型检查通过。没有运行全量测试或重构用户游戏。

真实页面复核：使用当前配置的 CodeBuddy / GLM-5.3-Flash，从 test123 的实际工程生成了 8 张源码绑定卡片与完整大纲草稿（revision 30）。已在浏览器核对可编辑字段、未决定事项及确认前禁用的卡片入口；尚未替用户确认策划或修改枪械。游戏源码未改动，当前结果等待用户审阅。

## 2026-09-09：记忆融入对话

消息内依据、即时记忆修订、候选确认、学习变化和撤销已接入；项目原始决定保持单一来源，类型化及原生实际请求记录独立快照。隔离 API、后端及组件检查通过，真实模型未调用。完整仓库检查的既有阻碍与验证边界见 [对话记忆](docs/conversation-memory.md)。
