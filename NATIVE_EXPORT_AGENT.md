# 导出页原生 Agent 接入（2026-09-08）

导出页现在通过当前配置的 Codex / CodeBuddy CLI 原生执行任务。它可以实际执行本机命令、读写独立导出工作目录、访问可信下载源，补齐导出需要的工具与 SDK、修复打包配置，再继续构建。提示词和按平台加载的技能通过系统指令通道传入。

## 使用

新建导出默认选择 Agent 导出。对话默认「操作电脑并继续导出」，点击「发送并执行」即授权当前显示范围；可切换「仅讨论」。模型选择器复用应用现有提供方设置。兼容 API 提供方不具备原生命令工具时明确提示选择 CLI，不静默改用另一提供方。

执行记录、最终消息、原生任务 ID、失败与取消状态保存在原导出页，刷新可以恢复。后续消息携带已有工作目录、历史与实际日志，创建新的明确执行轮次，不重放旧原生命令。固定构建选项仍可用，且与原生执行互斥；其依赖下载开关仅约束固定流程，不覆盖本次原生授权。

## 执行链

ExportService → NativeExportPort → AgentTaskService.prepare_export_task / run_export_task → project-export-agent 授权与 Harness → ProviderService.execute_task → 当前 CLI。

专用 profile 绑定服务端登记的 export_id 与隔离目录，不使用 project-demo-agent 的游戏初始化和预览交付流程。复用已有任务授权、调用记录、期限、取消与占用机制；只有专用导出范围允许环境补齐，普通原生任务的原有限制不变。

原生任务统一运行在应用既有事件循环，后台导出线程通过线程安全调度等待，关闭服务时允许取消与清理完成；不建立会干扰后台学习或关闭流程的临时循环。

Agent 写 sceneops-export-result.json 登记本轮产物。Build Release 校验平台、目录、文件及 APK/ZIP 基本结构，复制到独立历史产物目录供下载。CLI 结束与包已生成、设备验证通过分别表示；设备交互仍须真实验证。

## 验证

- 真实 CodeBuddy 原生调用完成：导出页消息 → 授权任务 → 执行 `node --version` → 写入 native-command-check.txt → 原生状态 succeeded → 返回对话。
- 读回测试文件内容为 `v22.22.0`，持久化 70 条操作事件；任务 ID 为 `task_0b60c6d3041a482090c40dc4c748420f`。
- 测试目录：`/private/var/folders/my/6wlls8751xgdx2cvhq5d7fvh0000gn/T/sceneops-native-export-live-zu1m0aut`。测试只运行版本检查并写两个文件，未安装系统工具、构建应用或修改用户游戏。
- 原生生命周期与取消 5 项、提供方权限 2 项、实际 HTTP 组合 2 项、导出后端 16 项、前端组件/传输 6 项定向测试通过。导出组件局部严格 TypeScript 检查通过。
- HTTP 组合测试验证真实任务授权链、应用事件循环、事件回传、产物导入下载和取消；仅模型提供方使用注入夹具，不以夹具 ZIP 声称真实可玩。

## Limitations

当前复用原生 CLI 的命令、文件和网络能力，不宣称具备尚未连接的任意桌面 GUI 控制。登录、许可、管理员交互、付费与公开发布仍遵守对应授权边界；不绕过系统安全检查。真实验证覆盖 CodeBuddy 命令执行；Codex 通路经过配置/权限测试，未在本轮调用真实 Codex 模型。

本轮没有安装 Android SDK，也没有重新进行各平台游戏验收。此前技能说明中“原生导出尚未接通”的限制已由本次接入取代。
