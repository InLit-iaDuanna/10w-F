# 拉出区域与聊天交互：功能回归

2026-09-05。用户明确扩大授权到 UI 功能测试，并要求拉出后立即看到功能、聊天参考 Codex 输入体验。本记录取代上一轮仅烟测的验证范围，不代表完整制作链验收。

## 修复

- 功能选择器删除介绍页：搜索/位置栏固定在顶部，功能列表独立滚动；矮区域隐藏次要说明。拉出至少保留 180px，第一屏可直接选择。
- Dockview 8.2.0 的边缘组未订阅公开 `setSize` 事件。使用 pnpm patch 补上四个发布 bundle 的事件通路，遵守引擎上下限与订阅释放，没有升级依赖或改掉许可证。
- 模式同步改为幂等，避免重复 collapse 清空 Peek 内容；空区域先装载编辑器。尺寸来自原生序列化的 expanded size，不再把 32px 折叠标签栏写成上次展开大小。
- 修复双击被两次零距离 pointer-up 当成关闭拖拽的问题；取消、丢失捕获和其他指针不会提交旧拖拽。
- 失败/加载中的编辑器保留选择、关闭和重试区域栏；隐藏面板不进入键盘/辅助技术交互。重置主页移除空边缘组。
- 角色模块改为显式 `zod/v4`，避免 Vite 预打包不同主版本时 `.loose` 不存在导致整个入口无法导入，校验语义不变。
- 聊天改为紧凑消息流和底部输入框，输入高度随内容增长。Enter 发送、Shift+Enter 换行、中文 IME 保护，保留原取消/失败草稿/重试与明确上下文边界。

参考了 [官方 Codex 仓库](https://github.com/openai/codex) 与 [开源 ChatComposer](https://github.com/openai/codex/blob/main/codex-rs/tui/src/bottom_pane/chat_composer.rs)。公开参考是 Rust 终端输入器的尺寸、输入、提交及草稿处理；本项目用 React/CSS 实现，没有复制桌面 App 源码或接入 Codex 模型。

## 实际验证

独立临时数据 `/tmp/sceneops-ui-functional.NH4DzW`、4302/8302、独立浏览器会话；用户 4301 数据和布局未重置。Playwright CLI 真实鼠标拖动与点击，不只运行状态函数。

| 范围 | 结果 |
| --- | --- |
| Shell 单元、状态、持久化 | 40/40 通过 |
| Dockview bridge / 安装引擎尺寸 / pointer | 25/25 通过 |
| 角色模块命令、入口、编辑器、边界 | 11/11 通过 |
| 对话旧模块测试 | 40/42 通过；两项因 Node strip-types 无法直接加载 TSX 失败 |
| Shell tsconfig 跨模块类型检查 | 仍失败，265 条诊断；此前为 277。未宣称类型全过 |
| 四边 190px 临时展开 | 全部第一项可见；Escape 收起；保存尺寸均为 190 |
| 四边 280px 固定展开、双击收起 | 全部通过 |
| 顶部短拉 80px | 最小展开 180px，第一项可见，不需滚过说明文字 |
| 顶部改为 360px 并刷新 | 保留 pinned 与 360px |
| 13 个入口原位选择 | 本地项目、计划、10 个业务工作台、命令搜索全部空态可打开；始终两个标签，无额外副本 |
| 10 业务入口的动态导入 | 全部成功；首次遍历发现并修复角色入口失败 |
| 搜索 | 用途“衔接”命中概念与资产；无匹配显示空态；清空恢复列表 |
| 浮动窗口 | 本地项目在浮动组打开，关闭后浮动组移除 |
| 重置纯对话主页 | 通过，且不保留空边缘占位组 |
| 聊天输入、失败与重试 | Shift+Enter 换行、IME Enter 不发送、普通 Enter 请求、503 错误可见、草稿保留、手动重试、空输入禁用均通过 |
| 聊天取消 | 请求中取消按钮与取消状态通过 |
| 窄屏/宽屏 | 776×673、1280×720 实际截图检查；极矮面板仍可滚动到控件 |

聊天故障与取消测试在浏览器拦截 `/api/ai/chat`，明确标为 Mock UI 回归，不发送到后端或真实模型。未把模拟结果保存到用户数据库。重启临时服务时也实际观察到 API 未就绪的 502，随后手动重读成功。

截图在 `output/playwright/functional-*.png`（忽略 Git）。`functional-chat-wide.png` 是最后的纯对话图；`functional-top-final.png` 是顶部大区域与矮聊天并存图。其余 debug/before 图记录修复过程。

## Limitations

未进行真实 AI 推理、付费调用、生产案例、Unity/Blender/渲染/playtest、生产构建或完整项目业务流程。未穷举多显示器 popout、所有拆分/合并组合及跨浏览器兼容性。全项目类型错误和对话旧测试运行器问题仍待后续处理；本轮不能作为全产品验收。

## 维护

`patches/dockview-core@8.2.0.patch` 由 pnpm 的 `patchedDependencies` 应用。此次 patch-commit 使用缓存、零下载、禁安装脚本。升级到上游修复版本时，应移除 patch 并保留四边尺寸/Peek/折叠记忆回归；不要通过修改应用 DOM 几何代替修复。
