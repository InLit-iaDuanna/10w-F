# 单人协作策划：阶段一

## 最新：Git 卡片分支

制作卡片现为横向分支入口，首次点击创建真实 Git 分支/worktree，再次进入复用目录。正式策划版本已有 Git 提交/标签；大纲/卡片修改提案支持预览和确认采用。原文“不是 Git 管理”的阶段限制由本增量覆盖；自动合并和开发执行仍未接入。见 `GIT_CARD_BRANCHES.md`。

## 对话交互修正（Markdown / 选项 / 卡片 / 流式）

回复使用共享 Markdown 渲染器，支持标题、列表、代码和表格；不执行 HTML，不自动加载模型给出的外部图片。旧回复保留文字内容，只改变呈现。

新 grill-me 回复使用单个 `PlanningQuestion` 和2至3个选项的结构化合同；推荐项可预选，但确认按钮才提交。用户也可在主输入框自行补充。问题和回答通过消息 ID 关联，重复回答旧题被拒绝。旧历史不猜测转换成选项。

制作卡片直接呈现名称、说明、待制作状态和可展开完成条件，不打开独立功能页、不启动生产。新生成提示按地图/角色/玩法/反馈等高层制作线组织，不要求逐项技术配置。

新增 POST `command/stream` SSE：正文增量、提供方思考内容、状态、最终已保存结果和错误。只展示实际事件；最终结果替换临时流。结构化 JSON 不作为原始正文显示，问答卡片验证后才显示。思考内容可折叠；未返回时不伪造。临时流不作为已保存历史，刷新后只恢复正式结果。

已运行4项传输 fixture、4项策划边界烟测、2项 Markdown/选项组件烟测；未发起新的真实模型推理。页面检查了已有 Markdown 回复和紧凑输入框。CodeBuddy 真增量协议通过本机帮助/已安装代码核对，真实账户端到端仍待用户发送验证；Codex 是完成项级事件，不承诺逐 token；标准 OAI SSE 未暴露思考字段。

## 最新界面收简

按用户追加要求，当前仅保留 Codex 式主对话、当前阶段、输入框和下一步。去掉横排步骤导航；大纲、版本、制作清单以折叠结果附件呈现，不展开每步骤功能页。制作卡片后端编辑合同保留，但复杂逐卡编辑 UI 暂缓。新项目选择中目录树默认折叠，旧入口置于高级项。委派仅在必要时使用 Astra / low。

## 操作旅程

1. 打开本地项目，浏览目录或输入绝对路径；填写新子目录名称，创建文件夹项目。
2. 项目进入唯一主对话的协作策划界面。模型沿用所选 CodeBuddy / Codex / OAI，不自动切换。
3. 聊 idea，点击「我说完了 · 开始对齐」才进入 grill-me。每次模型回复要求只问一个问题并给建议；真实质量由所选模型决定。
4. 点击「细节已对齐 · 生成大纲」，编辑目标体验、核心循环、范围与验收条件。未确认假设单独展示，建立版本前需要审阅。
5. 确认策划 v1，明确确认 Three.js 路线，再请求模型生成制作卡片。卡片支持增删、编辑、依赖关系与完成条件，状态始终为待制作。

输入和有效文档草稿自动保存；输入不完整时保留编辑和未保存提示。刷新恢复持久内容。旧项目、布局、历史记录均保留，不自动迁移 Unity 工程。

## 存储与接口

- `sceneops_project_workspace` 公开文件夹浏览、项目绑定和固定用途设计存储接口。只在用户选择父目录下新建独立子目录，不覆盖已有目录。拒绝符号链接目录。
- `.sceneops/design/draft.json` 保存工作稿；`.sceneops/design/snapshots/vN.json` 为用户确认的不可变策划版本。
- SQLite 保存项目绑定、策划消息、阶段、命令身份、模型请求记录和待导出状态。失败不生成 Mock 回复；模型调用日志只记录提供方、模型、时间，不记录凭据。
- 同一请求 ID 不允许改内容；最终提交比较原始 revision。跨进程更新冲突返回 409，不覆盖新内容。
- 磁盘导出前持久化完整待提交状态。中断后下一次显式命令先核对并完成同一导出，不重放模型请求。GET 和启动不自动制作。
- `GET /api/design/journeys/{project_id}`；`POST /api/design/journeys/{project_id}/command`，操作由 `JourneyCommand` 定义。Schema 生成：`node scripts/python.mjs scripts/export-journey-contracts.py`，然后根 `pnpm exec openapi-typescript modules/design-room/contracts/journey.openapi.json -o modules/design-room/frontend/src/generated/journey-api.ts`。
- `SCENEOPS_PLANNING_JOURNEY_ENABLED=false` 关闭新策划后端；不删除记录。

## 验证

只运行目录与策划的最小相关烟测；模型使用隔离 fixture，不发起真实推理。覆盖创建/重开、不可覆盖、符号链接拒绝、完整策划到卡片、请求重复、旧 revision、快照导出中断恢复和两实例提交冲突。

页面烟测在应用 `.local/planning-journey-ui-smoke` 创建隔离空项目，验证新入口、单人阶段和输入草稿；此项目不是游戏 demo，也没有模型回复。

## Limitations

- 当前正式版本只包含策划、技术路线及卡片；资产、空间和代码将在后续阶段纳入。不是完整 Git 项目版本恢复功能。
- GLB 导入/归一化、空间编辑、截图标记、Three.js 运行与 Agent 组装尚未接入这条新旅程。
- 未做新旅程真实模型对话质量验收、生产构建、Unity/Blender 或自动游测。
- 全仓 TypeScript 检查存在历史错误，不能宣称全仓通过；本次新增页面的报告错误已修正。
- 文件夹浏览是本地 Web 内的目录选择器，不是系统原生对话框；旧工程不自动绑定或接管。
