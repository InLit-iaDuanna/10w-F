# 当前 AI 调用边界

以下是调用边界，不是完整费用统计。初始交付没有模型请求；追加授权后已执行限定的 GLM/HY4 连通检查，成功与失败记录见 `AI_LIVE_VERIFICATION.md`。

| 入口 | 模型行为 | 触发与预算 |
| --- | --- | --- |
| 首页聊天 | 使用应用保存的历史和选定上下文 | 用户发送；独立请求超时/取消，无自动重试 |
| 模块建议 | 模块业务提示词 + 当前上下文 | 手动请求并手动采用，不执行外部操作 |
| 生产计划 | 意图理解 + 结构化步骤提案 | 用户生成；两次调用，总路由超时 120 秒，不在后续 Run 预算内 |
| 专家步骤 | 10 角色中的指定角色输出分析 | 用户开始可执行计划后；受持久 Run 调用/时间/重试限制 |
| 恢复建议 | 根据真实失败记录生成建议 | 用户手动请求；一次调用，不在原 Run 预算内，不自动实施 |
| 观察 | 确定性运行事实 | 不调用模型 |
| 模板提取 | 已成功运行的结构化草稿提取 | 不调用模型，不自动重放 |
| 模型目录/健康接口 | 本机配置/候选读取 | 不发送推理，不证明认证成功 |

## Provider 与安全

统一入口 `sceneops_ai_provider.ProviderService`。CLI 默认不传 `--model`，显式选择才传；禁用工具、严格空 MCP、空临时 cwd、不持久 CLI 会话，不使用 Bridge/bypass。CLI 使用专用系统提示；结构化输出是纯 JSON 与应用端标准 schema 校验，不使用挂起的 CLI `--json-schema`。兼容服务明确配置 URL/Key，关闭重定向与环境代理，返回内容不执行为代码。

Key 按精确 endpoint 保存于 `0600` 文件，只写 API 不回显；更改 URL 不复用其他 endpoint Key；一次请求用同一配置快照。原始 CLI stderr/stdout 不作为公开错误返回。Key 不进入前端查询/MutationCache 或浏览器持久化。

产品内角色与开发委派模型分离：开发采用 Astra/Sol/Terra 不会强行改变用户的 CodeBuddy 或兼容模型设置。模型层级可以手动覆盖，否则继承全局模型。

## Limitations

服务不提供费用时必须保留未知，失败/取消也消耗计数。默认未知计费阻止更多付费调用；明确选择 bounded_calls 才继续至次数/时间边界。这不是已验证的金额或 token 硬上限。结构化响应仍需供应商支持 Chat Completions `json_schema`，本地按 Pydantic 校验，不静默改成 Mock 或忽略校验。
