# 侧边试玩与本地服务

更新日期：2026-09-08。

游戏制作结果中的“游戏试玩”通过工作台打开右侧编辑器分栏，按任务选择对应预览。再次打开时聚焦已有试玩窗口。预览使用 iframe，工具栏中的“单独弹出”可打开独立标签页。

顶栏“本地服务”打开服务管理面板，每 3 秒刷新，也支持手动刷新。列表显示进程、监听端口及可关联的项目。关闭前显示确认；后端重新核验进程身份和监听端口，再发送 SIGTERM。系统服务、其他用户进程和当前工作台服务受保护，不提供强制终止。

接口：GET /api/agent/local-servers；POST /api/agent/local-servers/{server_id}/stop。接口沿用本地来源和令牌验证。后端实现详见 modules/ai-agent-runtime/docs/LOCAL_SERVERS.md。

验证：3 项前端定向测试、2 项后端测试通过。实际页面验证右侧试玩、键盘操作及独立弹出入口；默认试玩没有新增浏览器标签。分别通过 API 和页面关闭专门创建的临时 Python 服务，确认监听端口从列表消失；工作台 4303 和 8303 端口受到保护。

## Limitations

本地服务发现已在 macOS 上验证，依赖 lsof、ps 及当前用户的进程权限。完整生产构建因机器负载和模块加载问题取消；本次验证覆盖定向测试及实际开发环境交互，不代表完整生产构建通过。
