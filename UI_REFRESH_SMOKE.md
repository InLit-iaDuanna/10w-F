# V5 统一 UI 更新与烟测

日期：2026-09-05。用户授权重做视觉，不要求沿用旧外观；保留现有交互逻辑。

## 本轮交付

- 石墨灰底色、统一蓝色交互重点，48px 顶栏始终提供本地项目入口。
- 紧凑欢迎区、文档式回复、单一输入面板，模型/提供方/明确选中上下文位于输入下方。
- 36px 区域标题选择器，图标快捷操作与更多菜单；原四边拉手、停靠标签、原位切换不变。
- 中文分组工具库、用途描述、名称/用途/类别搜索；其他打开位置收进折叠选项。
- 提供方原生弹窗保留 CodeBuddy CLI 与 OAI 兼容 URL/API Key 配置，密钥仍只写。
- 简化生产计划未选择项目空态；不显示虚构运行进度。

## 已执行的最小检查

使用已安装的 Playwright CLI，以独立浏览器会话和临时空数据目录检查。没有安装依赖。
CLI 包装器依赖解析曾挂起，停止该次自有解析进程后使用本机已有的同一 CLI。

| 检查 | 实际结果 |
| --- | --- |
| 临时 Web 4302 / API 8302 启动 | 成功；数据目录 `/tmp/sceneops-ui-smoke.fgwHwk` |
| `GET /api/health` | 200；ready / sqlite / empty / external_execution:not_started |
| 首页空态与模型目录读取 | 无项目、无历史；CLI 默认模型及候选目录显示；没有推理请求 |
| 1280×720、776×673 视觉检查 | 欢迎、输入、发送及顶部项目入口可见；窄面板控件重排 |
| 左侧拉出 → 工具库 → AI 生产计划 | 在左侧原区域替换，右侧聊天保留，没有新增计划标签 |
| 区域更多菜单 | 原跟随/固定、标签、拆分、浮动、最大化入口可见；未执行业务命令 |
| 提供方弹窗 | CodeBuddy 与兼容服务字段可切换；未保存、未填密钥；Escape 可关闭 |
| 关闭左侧功能 | 返回单对话布局；仅修改隔离浏览器会话的布局 |
| 控制台 | 仅原 Dockview 许可证 error；另有浏览器 autocomplete 建议，无应用运行异常 |
| 用户预览 4301 | HTTP 200，保留服务与 `.local/v5-preview` 数据 |

本轮修正了小窗口欢迎区拥挤、发送按钮与右下角标识的重叠，以及用途搜索文案与查找范围不一致。
布局仍由 Dockview 原生机制负责，未隐藏或绕过评估水印。

截图保存在忽略 Git 的 `output/playwright/`：`ui-refresh-home.png`、`ui-refresh-compact.png`、
`ui-refresh-tools.png`、`ui-refresh-workbench.png`、`ui-refresh-provider.png`、`ui-refresh-menu.png`。
工具库截图为本轮中间状态，其余截图包含收尾的紧凑排版。

## Limitations

只做启动和 UI 空态烟测，以及 `git diff --check`。没有运行测试套件、类型检查、生产构建、
AI 请求、项目创建、设置保存、demo 导入、业务操作、Unity、Blender、渲染或 AI playtest。
不把候选模型目录、CLI 安装状态或界面模式视为登录/额度/真实连接成功。

本轮刷新共享外壳、对话、工具选择和计划空态，并未重写全部领域编辑器内部。
真实历史长文本、实际错误/取消/重试、带项目上下文、所有多窗布局尚未逐项验证。
此前真实 AI 连通记录见 `AI_LIVE_VERIFICATION.md`，不是本轮新增验证。

用户继续从 `http://127.0.0.1:4301/` 体验；本轮临时 4302/8302 服务在收尾关闭。
