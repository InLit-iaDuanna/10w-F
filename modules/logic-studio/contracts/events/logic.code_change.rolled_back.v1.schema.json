{
  "x-event-type": "logic.code_change.rolled_back",
  "x-event-version": 1,
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sceneops.example/schemas/logic-studio/events/logic.code_change.rolled_back.v1.schema.json",
  "title": "logic.code_change.rolled_back@1",
  "type": "object",
  "additionalProperties": false,
  "required": ["event_id", "event_type", "event_version", "occurred_at", "project_id", "correlation_id", "causation_id", "actor", "mode", "payload"],
  "properties": {
    "event_id": {"type": "string"},
    "event_type": {"const": "logic.code_change.rolled_back"},
    "event_version": {"const": 1},
    "occurred_at": {"type": "string", "format": "date-time"},
    "project_id": {"type": "string"},
    "correlation_id": {"type": "string"},
    "causation_id": {"type": "string"},
    "actor": {"type": "object"},
    "mode": {"enum": ["live", "cached", "mock"]},
    "payload": {
      "type": "object",
      "additionalProperties": false,
      "required": ["change_set_id", "receipt_id", "reason", "rollback_succeeded"],
      "properties": {
        "change_set_id": {"type": "string"},
        "receipt_id": {"type": "string"},
        "reason": {"type": "string"},
        "rollback_succeeded": {"type": "boolean"}
      }
    }
  }
}
