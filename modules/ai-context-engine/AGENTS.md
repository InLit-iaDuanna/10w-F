# 项目上下文

Own only this vertical capability and its public package. Runtime contracts come from sceneops_harness; providers from sceneops_ai_provider. Never run model text as commands. Expose only public imports, keep project scope, and report planned/live/mock honestly. Network types are generated from Pydantic/OpenAPI.

Only root performs the current user-authorized smoke. Maintain tests but do not run suites, demo, AI, Blender, Unity, build, render or playtest without explicit authorization.
