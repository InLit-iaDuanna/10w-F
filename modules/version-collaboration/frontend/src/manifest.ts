import type { ModuleManifest } from "./contracts.ts";

export const manifest: ModuleManifest = Object.freeze({
  schemaVersion: 1,
  id: "version-collaboration",
  version: "0.1.0",
  featureFlag: "version_collaboration",
});
