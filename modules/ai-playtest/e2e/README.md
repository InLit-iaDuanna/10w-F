# E2E 状态

模块本地套件定义了 hero 合同流程：钥匙—门尝试、结构化 evidence、组件 backpin、人工确认、修复后同 TestCase 复跑和 before/after 比较；Warehouse Escape 使用独立配置验证复用边界。

Dockview 中实际点击六个编辑器、Unity Live Game View、真实截图/轨迹恢复和跨模块 ChangeSet 审批 E2E 目前为 **Blocked**：本规范起点没有 `apps/web`、`module-runtime`、`engine-unity`、生成 API client 或 ChangeSet owner。此目录不放置伪造的浏览器/Unity 测试；集成这些前置模块后，应添加 Playwright hero flow、Warehouse Escape flow 与 cached judge-demo flow。
