[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"
[project]
name = "sceneops-ai-run-observer"
version = "0.5.0"
requires-python = ">=3.12"
dependencies = ["sceneops-harness==0.5.0"]
[tool.hatch.build.targets.wheel]
packages = ["src/sceneops_ai_observer"]
