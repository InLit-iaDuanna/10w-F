# 运行观察

V5 AI Harness 垂直模块。公开 Python 包：`sceneops_ai_observer`；Pipeline 合同来自 `sceneops_harness`，不另复制网络模型。

默认不执行；由明确用户请求或已授权的类型化 Pipeline 调用。真实外部能力不因本模块存在而变成 Live。遵守项目隔离、取消和预算边界。测试执行状态：not run，仅主代理进行最小烟测。
