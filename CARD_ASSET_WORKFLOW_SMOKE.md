# 卡片模型工作流烟测

日期：2026-09-06

## 用户路径

1. 在“制作卡片”中选择卡片，应用创建或复用该卡片的 Git 分支与工作树。
2. 选择“导入已有模型”或“新建模型”。
3. 导入路径选择 GLB/FBX 并点击“导入并检查”；新建路径在唯一对话中按轮廓、比例、表面和交互四个大块逐步对齐，可选参考图。
4. 新建路径的每条新用户回答都触发一次结构化方案与 Blender 草稿更新；右侧 Three.js 面板显示真实 GLB。第一次成功生成 v1，后续成功回答在同一资产追加 v2、v3，不覆盖旧版本。
5. 同一条消息通过稳定消息 ID 去重。失败不会自动重试，用户点击“重试本轮草稿”才重新调用模型和 Blender。
6. 成功后直接查看 GLB、真实尺寸、顶点/三角面、Blender 版本，并下载 `.blend`、GLB、FBX 或原文件；确认的版本可存入项目资产库。
7. 存入资产库后可“继续新建”另一个模型，或进入 Three.js 环境场景。环境场景支持从资产库人工加入、选择和微调对象；选中场景模型后，下方资产卡片会切换为该模型的信息与变换编辑器，也支持在同一个主对话中让 AI 生成新场景版本。
8. 需要统一比例时填写目标最大边（米），点击“另存归一化版本”。旧版本和原件不覆盖。

## 本轮实际结果

- 真实 Blender 5.1.2：在隔离临时 Git 工程中完成原语模型新建、GLB 导入、FBX 导入、2 米最大边归一化和双格式再导出；源文件保留，Git 没有自动提交。
- 真实 CodeBuddy：`glm-5.3-flash` 在隔离临时工程中返回结构化低多边形模型方案并通过 Pydantic 校验；只调用一次，不执行工具。
- 实时版本链：隔离临时 Git 工程中连续两轮请求生成同一资产的 v1/v2；重复提交同一消息 ID 复用已完成结果，没有产生第三次模型或 Blender 调用。
- 真实端到端：`glm-5.3-flash` → 结构化方案 → Blender 5.1.2 → GLB 在隔离临时工程中通过。统一 Web 的 `planning-journey-ui-smoke` 测试卡片只新增了一个真实 v1，刷新、收起和重新展开均未生成 v2。
- 提供方图片合同：Codex CLI 使用本机图片参数；OpenAI 兼容的 Chat Completions/Responses 生成视觉输入结构；CodeBuddy 当前文字通道在调用模型前明确拒绝图片，不自动切换提供方。
- 统一 Web：4301 的健康、AI 设置、当前卡片和卡片资产读取接口返回成功；900px 与桌面宽度均显示唯一对话、右侧实时预览、四个对齐大块和版本选择。进入会话自动定位最新消息，收起预览后对话区扩展。
- 项目资产库：真实大树 v1 的 `.blend`、预览 GLB 与 FBX 作为同一不可覆盖版本保存；同一来源版本重复保存返回原记录，不产生重复版本。
- 人工环境搭建：从资产库加入大树生成场景 v1；把位置改为 `(-2, 0, 3)`、Y 旋转改为 `45°`、缩放改为 `0.8` 后生成场景 v2，API 回读与页面一致。
- AI 环境搭建：真实 `codebuddycli / glm-5.3-flash` 使用唯一主对话和当前资产库，在保留原对象的前提下添加左右两棵树，生成场景 v3；Three.js 实际显示 3 个 GLB 对象，模型没有执行文件或 Shell 工具。

本轮修复了 Blender 新建操作名映射、Blender 5.1 FBX 运算符兼容、AI 设置字段 422、失败版本覆盖风险、二进制上传统一错误边界、失败后资产/日志不刷新、旧对话刷新误触发生成，以及进入长对话没有定位到最新消息的问题。

## 未运行与边界

- 未运行游戏 Demo、Unity、构建、渲染、AI 游测或完整项目测试套件。
- 未自动压缩 GLB；压缩是保留原件后的显式后续步骤。
- 未自动提交或合并 Git。模型只有在用户明确“存入资产库”或“加入场景”后才进入相应记录。
- 本轮环境场景是 Three.js 项目草稿与真实 GLB 预览，不等同于已经写入 Blender 或 Unity 工程。
- Dockview 开发许可证水印仍存在，不影响本轮模型工作流。
