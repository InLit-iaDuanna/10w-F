# 四边交互、即时聊天与本机诊断（2026-09-05）

本轮按用户追加的 UI 功能测试授权执行；不运行游戏案例、真实 AI、生产构建或外部工具。

## 已修正

- 普通四边拉出为固定区域；取消 640px 上限，按画布边界计算位移。初次展开最小 180px，原生分界线可缩至 12px 并在释放时收起。显式菜单/键盘临时展开仍保留。
- 原生布局通知只读回测量结果，不在 React 每次渲染后再次写入尺寸。隔离页复现了上下区域争抢空间造成的无响应；改为仅在 drawer-change 命令时写入后，相同操作可完成。
- 满屏时相邻分界线重叠，禁用分界线不再拦截可用分界线的指针事件。
- 全部编辑器关闭时，在同一关闭事务中恢复中央聊天；清除没有标签的边栏，已移除的原生边栏元数据归为隐藏。脏内容确认、关闭历史与撤销保留，不清理项目数据。
- 拉手接近区扩大，轻微放大使用 240ms 非线性缓动；尊重 reduced-motion。
- 发送后立即显示临时用户消息、清空输入并允许继续写下一条；等待、失败与取消都有状态，重试针对原消息，不覆盖新草稿。成功以服务器历史替换临时消息。取消不保证服务端未完成，不自动重试。

## 诊断入口

右上角「诊断」显示本机最近 200 条操作/异常摘要、浏览器和视口信息，支持复制、导出 JSON、清空。当前面板展示最近 80 条，导出包含完整缓冲。

存储键 `sceneops.ui-diagnostics.v1`，按浏览器 origin 隔离，刷新保留；不自动上传。仅允许边、尺寸、阶段、编辑器 ID 等非内容字段；错误保存类别与去除 URL 查询参数的脚本位置，不保存原始错误消息、聊天正文、请求体、密钥。无存储权限时仅保存在内存。不主动创建 WebGL 上下文。

浏览器渲染进程硬崩溃或主线程卡死不保证产生 JavaScript 异常；可通过刷新/重新打开后查看最后操作。不能把诊断面板的“0 个错误”当作浏览器从未崩溃的证据。

## 本轮验证

- Forge Shell 模块 46/46；桥接/已安装 Dockview 边栏通路 33/33；诊断 5/5；聊天临时状态 3/3。
- 隔离 4302/8302、临时数据目录；真实鼠标完成上下左右拉满与分界线拖回收起。底部到顶部实测区域高度 672px（1280×720 视口，48px 应用栏）。已有对向边栏时受其折叠条占位约束。
- 关闭全部五个测试窗口后，实测仅一个中央聊天区域，占满 1280×672；保留未保存内容确认。
- 诊断实测读回之前无响应操作前的持久记录，并记录后续四方向收起事件。
- 独立 4303/8303 使用显式 Mock UI API 将请求保持等待：用户消息立即出现，下一条草稿可继续输入；释放为 503 后原消息可重试。内存记录确认两次发送正文相同；取消后下一条草稿仍在。没有调用模型或应用数据库。

## Limitations

- 用户旧 4301 标签已有浏览器“页面崩溃”状态，未获得旧渲染进程堆栈。本轮确实复现并修正了相邻边栏造成的 UI 无响应，不能据此断言覆盖所有历史崩溃原因。
- 类型检查仍有 262 条跨模块诊断；本轮新增诊断组件、聊天发送实现、Dockview 桥接和指针文件的定向筛查没有残留报错。没有声明全项目类型检查或完整测试通过。OAI 服务、真实 AI、业务案例和生产链本轮未验证。
- Dockview Enterprise 仍为本地评估，保留官方水印；受控 Core 补丁的范围见 THIRD_PARTY_NOTICES.md。

## 交付预览

4301/8301 已使用原 `.local/v5-preview` 数据重启，Web 与 `/api/health` 返回 200。旧标签无响应，另开 4301 标签确认历史对话与模型选择仍在，展示「诊断」入口；未重置用户布局、设置或项目。4302/4303 的自有临时进程已停止。
