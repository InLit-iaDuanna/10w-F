# 中转 GPT 原生执行

用户选择 OpenAI 兼容服务并使用完全访问时，制作与导出复用 Codex CLI 原生能力。普通讨论沿用原文本接口设置；原生制作使用保存服务的 `/responses`，需要中转支持 Responses 和工具调用。不会更换模型、使用官方登录或回落到结构化动作循环。

## 隔离

每次调用创建临时 HOME/CODEX_HOME，不加载全局配置、rules、MCP、插件、hooks。真实 Key 留在后端，CLI 只收到本次固定目标与模型的临时本地凭据。Shell 使用显式环境白名单，不继承通道凭据。转发仅接受 Responses 与 compact，检查认证和模型、拒绝重定向，不转发客户端 Cookie、任意目标或上游错误正文。

任务准备记录非秘密端点，执行前再次比较；端点改变需要新授权。结束或取消停止进程组，关闭入站/上游连接并清理临时目录。原生输出逐行处理，取消旧的累计 4 MiB 输出限制，受限文本调用保持原限制。

## 默认与界面

默认内部模型请求、动作次数不限，总运行时限为 null；用户仍可显式设置时限。一次授权启动一个 CLI，会话结束不自动重试。外层 Harness 的会话启动次数不代表内部动作次数。

项目 Agent 直接展示真实活动，旧受控任务也显示持久化动作。原预算只作为旧任务历史。新原生授权仅在相同方向、工作区、无活动进程、无未决写入与工具清理不确定性时接管旧 typed 任务，保留源码、旧失败状态与计数。

## 验证

- 本机 Codex CLI 0.144.1＋本地模拟 Responses，使用最大思考强度：执行命令、写入文件、Shell 无临时凭据、上游测试 Key 未进入 CLI 环境、事件回传与目录清理，通过。
- 前端 API 原生准备、自动授权与后续会话定向冒烟，通过。
- 模拟 API 写入/回读源码，随后新原生任务授权交接，历史与源码保留、总时限无限，通过。
- 对话静态渲染显示文件、说明和当前状态，通过。

## Limitations

未调用用户真实中转模型、安装依赖、构建游戏或运行完整套件。Vitest 因已有 picomatch `scan is not a function` 未启动，未改动依赖。当前保存模型为 gpt-6-astra，总时限为 null。

这是配置与凭据传递隔离。danger-full-access 使用同一系统用户，不能阻止 Shell 读取主机其他文件，不是系统安全沙箱。单条异常巨大事件仍有读取上限；连接/空闲超时不是总任务时限。

依据：[Codex 配置参考](https://learn.chatgpt.com/docs/config-file/config-reference)、[配置与状态目录](https://learn.chatgpt.com/docs/config-file/config-advanced)。
