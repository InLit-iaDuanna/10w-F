# 主对话制作流程

2026-09-08。新项目现在通过同一条对话讨论想法、回答问题、开始制作并查看试玩。初版方向由模型生成结构化草稿；普通用户无需填写表单、选择代码架构、确认大纲或操作制作卡片。历史资料保留。

## 权限

- 输入框旁选择“执行前询问”或“完全访问”，按项目持久保存。
- 执行前询问：讨论不写游戏文件；制作前审阅本次范围，后续修改在主对话询问。
- 完全访问：用户开始制作或发送后续目标时明确授权当前任务，使用所选 CodeBuddy/Codex 原生会话；项目登记、提供方绑定、任务占用、取消和范围检查仍生效。模型回复不能修改权限。
- 原生任务结束后，在当前有效授权内由固定运行器执行依赖准备（仅已授权时）、检查、构建和预览；任一步失败即保留真实失败结果，不声称试玩成功。

## 验证

- 对话到摘要、不生成卡片、权限保存、旧问题拒绝、模型不能授予权限：2 项后端定向检查通过。
- 主输入框两种权限与一次开始制作：2 项组件检查通过。
- 无卡片原生项目授权、连续任务身份、作用域拒绝及自动交付顺序：2 项后端定向检查通过（替身执行，不调用模型）。
- GLM-5.3 真实链路已通过：主对话澄清 → 完全访问原生制作 → 自动检查/构建/预览；随后在同一对话直接修正高分屏裁切，无制作卡片或重复授权页。

## Limitations

完全访问沿用原生 CLI 任务权限，项目目录不是操作系统沙箱。自动检查和构建不等于玩法验收。仓库全量类型检查存在跨模块错误；本次生产入口未报告新增类型错误。旧测试项目文件已移到废纸篓，未删除历史用户项目。

## 系统级游戏生成要求

根据本次高分屏裁切问题，新增按游戏视角生成的系统提示：一屏包围盒取景或跟随玩家等相机策略、实际模型尺寸与世界范围、CSS/绘制缓冲区像素区分、DPR、resize、HUD 和输入状态。CodeBuddy 使用实际 `--append-system-prompt`，Codex 使用 developer instructions。提示词不会新增权限；视觉验证仍与编译结果区分。

### 系统提示词真实复测

GLM-5.3 在新摘要中选择 `camera_mode=fit-scene`，执行任务记录了 987 字的实际 `native_system_instructions`，含一屏取景、DPR 与宿主适配要求；CLI 系统提示通道由定向传输测试核对。复测目标只要求按系统规范核查，未提供具体修补代码。模型增量加入宿主 ResizeObserver、重开输入重置、HUD 窄屏换行和取景安全边距；应用实际检查、构建、预览均通过。

浏览器实测：1280×720 / DPR 1 时画布 CSS 和缓冲区均为1280×720；390×844 / DPR 1 同样匹配且无横向溢出；恢复903×781 / DPR 2时 CSS 为903×781、缓冲区1806×1562，场景完整可见。按键移动后玩家位置改变；收集并交付得分从0变1；R重开后得分0、货舱0/5、时间1:30。浏览器控制台错误0。跟随玩家、第一人称、横向跟随的规则已覆盖系统提示选择测试，本轮实际游戏采用一屏取景。

当前新版工作台：http://127.0.0.1:4303/；本轮试玩：http://127.0.0.1:61332/（本机服务运行期间有效）。
