# Agent-first 游戏制作 Harness：当前理解与下一步

日期：2026-09-06。

本文记录用户与实施方、GPT-6 Pro 多轮审阅后对齐的方向，以及随后完成的首个产品里程碑。长期计划与已经执行的结果分别标注；游戏代码架构选择和真实工程起点的实现证据见 `GAME_CODE_ARCHITECTURE_MILESTONE.md`。

## 1. 我理解的产品目标

我们要做的是由 Agent 主导制作的游戏 Harness：用户提出目标，AI 尽可能自行理解现有项目、选择工具、制作和修改内容、观察实际结果、处理失败，再交付可检查的成果。

用户不应该负责在策划、建模、资产库、场景、代码和检查面板之间逐步搬运任务。主对话承载目标、反馈和持续追加的要求；工具面板用于观察结果、查看差异、纠正方向和必要的精细编辑。

下一阶段的可靠性工作，是为了让 Agent 能承担更长的连续制作任务。评价进展时，看用户减少了多少制作接力、Agent 完成了哪些实际工作，以及失败后能否继续。

固定原语、制作配方和卡片模板是可用的制作工具，不是最终创作能力上限。当前实现的限制应如实展示，但不据此把最终产品固定为低多边形生成器。

## 2. Agent、工具与 Harness 的分工

| 部分 | 应承担的工作 |
| --- | --- |
| 主 Agent | 维持用户目标和项目上下文，理解需求，选择制作步骤，跟踪结果并持续修正 |
| 专业 Agent / 技能 | 在任务确实需要独立专业上下文、并行处理或复查时参与，按需加载领域知识 |
| 普通领域命令与工具 | 执行对象移动、文件读写、资产导出、事务提交、编译检查等明确操作，返回真实结果 |
| Harness | 关联项目、操作、授权、制品版本与验证证据，组织取消、恢复和交付 |
| 用户 | 给出目标与反馈，在范围不清、需要产品取舍或确实无法自主推进时介入 |

AI 根据证据做制作决策；持久化和工具执行使用明确的程序语义。一次对象移动无需完整 Agent 调度。开发本项目所用的子代理，与产品内的制作 Agent 是不同概念。

## 3. 连续制作应具备的行为

1. 理解当前工程和已确认需求，优先发现可以复用的资产、代码和场景对象。
2. 在已有授权范围内执行制作，保留可定位的操作和结果版本。
3. 读取结构、文件、日志、编译结果、截图或运行状态，判断实际变化。
4. 根据真实反馈继续修正，保留未完成部分和失败原因。
5. 接受用户中途补充；取消时区分停止请求、执行结束和已经提交的结果。
6. 中断后核查既有结果和执行状态，按原授权与重试规则继续。
7. 交付实际制品、变化说明与验证范围，关闭项目后能够重新打开并继续修改。

“待核查”应对应可执行的核查动作：读取操作记录、关联结果、日志和执行状态。Agent 在已有权限内主动找证据；必要事实仍无法确定时，说明具体阻塞。不能把未知直接当作失败重做，也不能把一个状态标签当作恢复已经完成。

## 4. 保留的基础与实施边界

保留现有项目和资产身份、版本历史、文件保护、Git / worktree 隔离、授权范围以及执行与验证分离的设计。新路径应逐步复用这些能力。

各领域保留自己的命令。先在一个实际长操作上补齐必要行为，再由第二个真实使用点决定提取哪些公共代码。短事务不需要迁入完整 Agent Task；当前没有全量迁移操作表或更换运行时的决定。

优先使用已有事务、主键、唯一约束、版本字段、类型和普通测试。不默认新增哈希、冻结契约、门禁、租约系统或调度框架；也不删除既有安全措施。

已有且仍有效的授权在原范围、预算和有效期内继续生效。明确、可逆且处于授权范围内的操作可以直接执行；普通讨论不应自动写入场景。范围或影响不清时说明变更，已明确确认的同一操作无需因实现调整而再次确认。

场景提交后的撤销属于新的领域操作，要考虑随后发生的修改；不能把已完成结果改成“已取消”来代替撤销。

## 5. 当前项目主线与长期方向

当前仓库记录的阶段主线是：文件夹项目 → 单主对话讨论 idea → 用户显式进入 grill-me → 大纲 v1 → 游戏技术方案 → 制作卡片。技术方案当前固定已接通的 Web + Three.js 工具链，并明确选择对象／组件式或 ECS · Miniplex；保留简洁的聊天入口与按需工具。

这次对齐没有要求切换引擎、删除 Unity / Blender 路径、增加固定工作台页面，或把当前 Three.js 阶段写成永久产品上限。最终引擎主次、交付物与平台策略仍随用户的产品选择推进。

AI 优先也不自动开启原本关闭的游测或扩展外部执行权限。已有验证记录只证明其中写明的项目、工具链和范围，不代表整个制作链已验收。

## 6. 本次已完成的本地核对

本地 HEAD 为 `43e26a21d77746ee6361e54580db6790ca7ea65a`，与审阅提交一致。开始工作时已有两个未提交修改：

- `modules/world-composer/frontend/src/EnvironmentScenePreview.tsx`
- `modules/world-composer/frontend/src/EnvironmentSceneWorkflow.tsx`

这些改动予以保留。预览生命周期问题后续应先读取现有差异，不能直接照旧报告覆盖实现。

已阅读项目开发约定、产品说明、执行计划和近期状态记录，并定向读取下列函数：

| 问题 | 当前源码观察 | 当前证据等级 |
| --- | --- | --- |
| SCENE-001 | `add_object()` 通过 `model_copy` 追加对象；`_save()` 再通过 `model_copy` 更新后序列化，所读路径没有追加总量检查或完整模型重验 | 已定位当前实现；尚未运行边界复现 |
| ASSET-001 | `normalize()` 在取得项目 / 卡片锁之前读取资产记录，锁内继续使用该记录 | 已定位当前实现；尚未运行并发复现 |
| ASSET-002 | `generate()` 在锁前读取方案，锁内沿用方案状态并创建、保存新的资产记录 | 已定位当前实现；尚未运行并发复现 |

当前观察支持继续核实审阅所述风险，不等于已经完成服务 / HTTP 集成复现。环境执行者归属、启动恢复和实际进程模型尚待本地检查。

## 7. 下一步：三项局部数据完整性核实与修复

### SCENE-001：场景写入边界

使用临时数据库和有效资产夹具，构造当前约束允许的 200 对象场景，通过真实服务入口追加第 201 个对象，检查异常、版本记录及后续读取。

若复现，局部补齐领域数量检查和必要的持久化前完整校验。验收要求是超限请求不新增不可读版本，原场景仍可打开。当前的 200 是现有约束，不是本轮要冻结的永久产品规格。

### ASSET-001：并发归一化

使用临时目录、现有 Fixture Blender 和确定性线程同步，核实两个重叠请求是否使用过期记录覆盖版本列表。测试关注成功版本及文件关联，不靠随机延时碰撞。

首选修复候选是在锁内重新读取参与更新的资产记录。验收要求是所有成功版本都被保留，或请求明确返回冲突；不能静默漏掉版本。单进程锁的结果不能被描述成已解决多进程竞争。

### ASSET-002：同一方案并发生成

核实两个请求读到旧 `planned` 状态时，第二个请求是否重复调用执行器，并覆盖首个成功资产的状态或版本列表。覆盖第二次调用成功及若再次调用就失败的情形。

首选修复候选是在锁内重新读取方案状态。重复请求可沿用现有明确拒绝，也可按现有接口语义返回原结果；不得重复制作并破坏首个成功结果。

上述三项可以局部完成，不以公共架构提取为前置。每项记录复现结果、最小改动、定向回归结果和剩余限制。

## 8. 下一步：环境生成的取消与恢复

先检查现有请求表、服务组装、启动器和模型执行方式，回答：

- 同一数据目录是否可能由多个 API 实例、开发重载实例或 worker 使用？
- 谁还可能为本次操作提交结果，API 退出后是否有独立执行者继续工作？
- 哪些记录已有明确结果关联，哪些旧记录可能落在分离提交窗口内？

复用环境操作已有身份和结果字段，按实际缺口确定最小改动。模型调用保持在数据库写事务之外；可以原子提交的场景版本和请求完成状态在同一事务中完成。

| 独立验收场景 | 应有结果 |
| --- | --- |
| 提交过程中中断 | 场景版本与完成关联不出现半提交 |
| 已提交但客户端未收到响应 | 同一请求找回原结果，不再次调用模型或创建场景版本 |
| 未提交且确认原执行者无法继续提交 | 明确收尾，按已有规则允许继续或重试，不永久保留虚假 running |
| 原执行者仍活跃或状态不明 | 观察、核查，不重复认领执行 |
| 旧记录没有结果关联但可能已有场景写入 | 核查历史结果，不能仅凭空字段自动重放 |
| 取消与结果提交竞争 | 依据服务端提交结果呈现已停止或已完成，已完成结果不被迟到取消覆盖 |

停止按钮应绑定对应环境操作。前端断开连接、请求取消、执行者停止和结果回滚必须分别判断。Blender 进程级取消只有在选定路径确实涉及它时处理，不顺带改造所有外部执行器。

## 9. 后续能力与复用研究

相机稳定、聊天恢复、自动保存编辑状态、坐标往返、制品采纳与备份继续保留为待核实事项，按实际影响和依赖安排。局部交互修复无需等待全部运行时统一。

外部参考分为技能、工具桥接、Agent 运行后端、生成服务和游戏 Harness。先找到真实制作任务的能力缺口，再核对候选项目的实现、接口、兼容性与采用范围。评审提供的仓库清单是研究输入，不是已决定安装、迁移或采用的依赖。

## 10. 用什么任务检验方向

示例目标：给现有关卡增加一种会巡逻、发现玩家后追击的敌人，外观与当前场景一致。

Agent 应能理解工程、复用或制作资产、修改行为代码、接入场景，在授权范围内读取实际效果并修正。用户中途调整要求后，Agent 应追踪受影响的内容；发生中断后，能核查并继续，最后交付可定位的结果和验证证据。

这是一项后续验收示例，不代表当前已创建该游戏任务或已授权执行其外部工具。实际验收项目沿用用户选定的主线。

## 11. 本轮交付与 Limitations

游戏代码架构里程碑已经完成业务代码、生成合同、定向回归、真实工程构建、浏览器运行和一次真实 Agent 增量开发验证。用户可以在第一次生成代码前选择对象／组件式或 ECS · Miniplex，系统会创建可运行工程，并把方案带入制作卡、worktree 和 Agent 上下文。旧项目不从 Three.js 猜架构、不自动重建或覆盖源码。

本轮没有扩展到其他引擎、架构迁移、自动合并、发布或 AI 自动游测。上文三项数据完整性与环境恢复议题仍是后续独立工作，不属于这次架构里程碑的已完成范围。

原审阅的 P0 定级已撤回；新增哈希、全量迁移、默认产品收缩等旧建议不作为默认实施要求。后续执行以用户最新要求、当前代码和实际验证证据为依据。
