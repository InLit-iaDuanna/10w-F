# External packages required by the source-based local API runtime.
# Workspace packages are loaded from their checked-in src directories by
# scripts/python-workspace.mjs, so the one-click launcher does not build wheels.
fastapi==0.128.8
pydantic==2.13.2
uvicorn==0.39.0
PyYAML==6.0.3
httpx==0.28.1
jsonschema==4.26.0
