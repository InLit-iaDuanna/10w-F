# 单一主对话生产工作台 · 分段交付记录

日期：2026-09-05。完整四阶段计划尚未全部完成；本记录不替代全链验收。

## 已落地

- 统一应用各模块默认页面改为生产状态、步骤、产物版本、证据标识与停止入口；聊天、模型选择、授权留在主对话。旧手动工作台和历史计划保留在高级区域，未清空历史布局或数据。
- 流程树、主对话任务时间线和模块共用项目快照与单个引用计数 SSE 连接。持久事件序号支持断线续读；刷新只恢复记录，不调用执行接口。
- 真实 Harness 动作投影为模块步骤。CLI 活动在运行中逐条持久化，并显示最近活动；模型报告与工具执行结果、产物存在、检查通过分别记录。
- 执行节点首次出现时在右侧结果组打开，复用已有面板。Dockview 使用原生 inactive 参数，不以先抢焦点再恢复的方式处理；同任务同模块在当前浏览器标签页关闭后不循环重开。
- SQLite 迁移解除每项目一个任务的唯一限制。项目写入占用通过事务获取；未确定的写入保留占用，不盲目重放。工程归属根据应用登记与历史执行证据识别，不接管任意现有工程。
- 产物按项目/源路径维持稳定 ID，每次明确登记保留独立版本副本，不使用新 hash。拒绝越界、符号链接、隐藏文件及复制时变化；单文件最多 512 MB；HTML/SVG 不作为活动页面内联。
- GPT 图片新增显式授权字段，只有 Codex 完全权限任务确认图片授权后启用原生图像能力；不自动换付费 API。尚未调用真实图像生成。
- Codex 接入显式 `sceneops-outputs.json` 产物清单，解决 Shell 保存文件没有 file_change 事件的问题。清单限制 1 MiB/100 项，严格校验格式和路径，只有实际文件登记版本；模块归属保持 reported/review_required，不代替独立业务检查。

## 公共接口

- `GET /api/agent/projects/{project_id}/production`：同事务读取任务、步骤、全部产物版本及事件游标。
- `GET /api/agent/projects/{project_id}/events?after=N`：持久事件分页。
- `GET /api/agent/projects/{project_id}/events/stream?after=N`：`production` SSE 事件，支持 `Last-Event-ID`。
- `GET /api/agent/projects/{project_id}/artifacts/{artifact_id}/content?version=N`：项目隔离的版本文件读取。
- 合同源为 Pydantic，已通过 `pnpm generate:agent` 生成 OpenAPI/前端类型。

## 已执行的最小烟测

1. `test_production_foundation_smoke.py` 单个本地夹具：旧唯一约束迁移、同项目多任务、原子占用、历史归属、不可确定写入拒绝、不可变版本、项目/链接拒绝、SSE 续读、步骤更新保留产物引用。通过。
2. `test_codex_task_smoke.py` 单个注入 CLI 夹具：明确授权才执行、运行中事件可见、实际夹具文件登记、CLI 结束仍为待审阅、禁止不确定重放。通过。未调用真实 CLI 模型。
3. Dockview 单个 `production auto-open` 用例：新增结果使用 inactive，未关闭或替换已有面板。通过。不是全面拖拽回归。
4. Web/API 启动及页面只读检查：4301 主对话一个输入框，概念资产区无重复聊天，流程树显示未执行，项目快照/SSE/模型列表均返回成功。保留原页面，新开更新页便于比对。
5. `test_output_manifest_smoke.py` 单个本地夹具：真实文件登记、缺失/越界/隐藏/符号链接/额外字段拒绝、模块仅标 reported、不泄露清单原文。通过。
6. Provider 单个分片 JSONL 流夹具：进程未退出时收到脱敏事件。通过。

全仓 TypeScript 检查仍不通过，含现有清单 nullable、生成合同可选字段、目标 lib、Three 类型和 Node 类型冲突；不能宣称全仓类型检查通过。新增生产前端文件未在检查输出中出现错误。当前修改范围的 `git diff --check` 通过；全仓另有旧 Dockview patch 的缩进告警，未擅自重写补丁。

## 仍待实现 / 未验证

- 通用目标到完整生产 DAG 的规划与分模块执行调度，当前仍复用顺序行动循环；Codex 任意活动默认归集成区，不能猜测成其他节点已完成。
- 同项目后续任务的真实 Blender/Unity 会话继续与资产身份回读；已完成存储/授权层，不据夹具声称真实 DCC 连续任务通过。
  当前受控模式准备和授权时明确拒绝非空工程续作；不削弱旧适配器的任务绑定检查。用户可明确选择 Codex 完全权限在应用登记工程中准备新授权，不会自动切换提供方。
- 原生 GPT 图片账户可用性、可靠图片产物交付；当前 CLI 版本不输出原生图像条目，不能把文本/模型自述当图片。
- 真正的 GLB Web 预览、角色骨骼/动画、玩法/UI/音频/VFX 的真实执行器与独立检查。
- 渲染、开发构建、AI 游测、修复及版本交付的完整真实执行接口。旧能力仍保留，未接入处理器继续如实显示未接入。
- 自动面板完整焦点/关闭/项目切换浏览器交互用例，网络真实断开与应用进程中断的全部组合。

没有自动运行游戏案例、生产构建、渲染或 AI 游测，没有启动新 Blender/Unity 会话，没有真实 AI 推理或图片生成。完整游戏仍由用户测试。

## 当前入口与后续顺序

继续使用 `pnpm dev`；现有预览使用 `SCENEOPS_WEB_PORT=4301 SCENEOPS_API_PORT=8301` 和 `.local/v5-preview`。不自动安装依赖、初始化示例或启动任务。

下一段先完成项目持续执行、生产步骤图和真正可预览的版本化资产，再接角色/世界/UI，最后接渲染/构建/游测/交付。每段只运行相关最小烟测，不扩大验证范围。

技能与依据：本轮按 OpenAI Docs 技能核对原生图像和非交互 CLI 合同，按 Unity MCP orchestrator 技能保留会话身份、真实状态回读与检查边界；未因此启动任何真实工具操作。[官方图像说明](https://learn.chatgpt.com/docs/image-generation)、[官方非交互模式](https://learn.chatgpt.com/docs/non-interactive-mode)。
