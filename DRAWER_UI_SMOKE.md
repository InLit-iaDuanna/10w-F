# 拉出区域原位承载 · UI 烟测

2026-09-05，用户明确要求：拉起的区域自己选择功能，并由该区域承载。此轮不沿用上一轮真实 AI 测试授权，不发起模型请求或制作业务。

## 实现

- 四边空区域统一显示功能选择器；默认在当前实例执行 `workbench.switch_editor`，切换功能而非另开右侧面板。区域标题栏可再次「选择功能」。
- 显式选择拆分、标签、浮动或弹出时才改变布局；拆分/标签以选择器实例为锚点。
- 已打开的单例移到目标区域，保留其状态。未保存目标需要确认，锁定目标仍禁止替换。
- Dockview 通过官方 `getEdgeGroup/addEdgeGroup` 与 `position.referenceGroup` 原生挂载，不先创建中心面板再模拟拖放。用户请求的 mode/size 贯穿空 group 创建和内容挂载，避免中间 collapsed 回报覆盖拉出指令。
- 选择器和生产计划按面板宽度响应式排版，窄面板不再采用整页宽度的留白/双列控制区。

## 实际检查

使用独立 Playwright 浏览器会话打开既有 4301 服务，不重置用户浏览器布局，不修改项目数据：

1. 双击左侧拉手，出现左侧「选择功能」，默认打开位置为「当前区域」。
2. 选择「AI 生产计划」，同一左侧区域显示其空态；中央对话保持独立。
3. 标题栏「选择功能」返回选择器，再选择生产计划；面板实例 ID 不变，只有一个左侧标签，无旧选择器残留。
4. 原生布局与保存的工作区均保持 left / pinned / 280px，切换前后尺寸相同。刷新后恢复同一承载区域。
5. 检查窄面板截图及搜索输入尺寸；可见内容没有此前逐字挤压的问题。Console 仅现有 Dockview 评估许可提示。

截图：`output/playwright/drawer-selector.png`、`output/playwright/drawer-hosted-function.png`（Git 忽略）。

## Limitations

只执行上述一条 UI 空态路径。四边/单例转移/脏状态保护测试已维护，但未执行单元套件、类型检查、生产构建或完整浏览器回归；未穷举实际拖动、浮窗和所有工作台内容。没有 AI 请求、案例导入、项目创建或生产操作。既有保存布局不做猜测性清空或重排，用户刷新后可重新打开所需边缘区域。
