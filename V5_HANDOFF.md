# V5 交付说明

日期：2026-09-05。分支：`codex/harness-v5`；从统一工作台提交 `34121bf` 延续开发。原工作树、未提交用户文件和历史数据保留。

追加状态：用户后来明确授权真实 AI 检查，CLI 数组解析与结构化路线已修复；GLM 已取得真实成功结果，失败及未验证项见 [AI 真实连通记录](AI_LIVE_VERIFICATION.md)。下文保留初次交付时的验证边界，不代表追加检查未发生。

## 已落地

- 在原统一应用内加入 AI Harness，没有拆出另一套产品或 iframe；保留 11 组工作台、对话首页、四边拉手和原生停靠。
- UI/UX 重做：深色中性底色、分层信息、统一项目选择、提供方设置、计划/运行/能力/模板面板。空态不冒充正在制作。
- 统一 AI Provider：默认 CodeBuddy Code CLI；支持明确配置 OpenAI-compatible URL、模型、Key。共用聊天/建议/计划接口，不回退 Mock；调用不获得文件或业务工具权限。
- 8 个 AI 模块：目标理解、上下文、模型路由、专家分析、Pipeline 编译、运行观察、恢复建议、模板提取。
- 独立类型化内核：公开能力注册、顺序 DAG、持久运行/事件/尝试、审批、取消、手动重试、回滚基础与未知计费记录。高风险边界已经独立只读审查并修复审查发现，不等同于运行验证。
- SQLite 保存新增提案、运行、事件、模型层级及派生记录。API Key 独立存储，按精确地址绑定；临时布局不塞入数据库。
- 后端公开模型生成网络类型；现有独立入口通过兼容层继续保留。

## 打开与操作

V5 预览：[http://127.0.0.1:4301](http://127.0.0.1:4301)。API 8301，数据目录为应用 `.local/v5-preview/`。代理没有主动停止原服务；收尾发现早先进程已结束、4300 不可连接，只重启 V5 预览，不动旧数据。若用户重新启动其他端口的旧进程，不应把其状态当作 V5 验证结果。

1. 先选择或创建空项目；创建项目不会导入示例或执行作业。
2. 在「提供方设置」保留 CLI 默认模型，或配置兼容服务。仅读取模型列表不会发送推理请求。
3. 点击「生产计划」，填目标并明确选择项目草稿上下文；生成时会调用真实模型。
4. 审阅提案、权限和阻塞项。生成不是执行，只有明确开始才创建运行。包含未接通外部能力的计划会被阻止。

这些是供用户手动使用的步骤，本轮没有替用户执行。安装、重启、数据路径说明见 [README](README.md)。备份时先停止对应应用进程，再复制其完整数据目录；该目录含敏感 Key。

## Limitations

- 只验证启动、只读接口和浏览器空态。没有运行 demo、业务操作、测试套件、类型检查、生产构建、真实模型、Unity、Blender、渲染或 AI playtest。[详细烟测记录](V5_SMOKE.md)。
- CLI/OAI 登录、额度、模型可用性、结构化输出、真实延迟和取消未验证。Key 文件是本地权限隔离，不是系统钥匙串。
- 13 项外部能力只有 planned 声明，无 V5 执行 handler，且不授予 `production:execute`。旧模块能力不因整合自动升级为已验证 Live。
- 十个产品专家目前只做结构化分析；vision/player 是路由层级，不代表已经接通图像输入、游戏操控或工具执行。
- 内核当前为顺序 DAG；高级图节点、缓存执行及未支持策略明确拒绝。多审批人回滚不支持；不确定补偿需要人工检查与新审批。
- 生成提案的两次模型请求、恢复建议的一次请求不在 Run 预算内。运行的按调用次数策略限制次数而非已核实金额；服务未报告费用时保持未知。
- 恢复只生成建议，不自动改写或执行剩余图。模板是已完成运行的确定性草稿提取，不是完整学习系统，也没有验证第二个项目的模板重放。
- 没有运行各模块新旧回归测试；测试文件的存在不代表通过。Dockview 保留现有评估水印。

下一步由用户先手动验证提供方和一个纯分析计划，再根据结果授权对应修复或特定测试；本轮不额外补齐外部生产链。
