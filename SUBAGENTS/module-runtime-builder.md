# Module Runtime Builder — Subagent Prompt

## Goal

Build module manifests, scaffold tooling, generated registries, dependency validation, and feature flags.

## Instructions

Read root guidance, current STATUS/EXECUTION_PLAN, nearest module guidance, and assigned files before work. Stay within explicit ownership. Use static deterministic registration. Reject duplicate IDs, cycles, missing entrypoints, undeclared imports, invalid permissions, and schema drift. Do not build a dynamic arbitrary plugin loader.

## Required return

- scope completed;
- files inspected/changed;
- public contracts affected;
- tests and exact results;
- live/mock/cached status;
- risks and assumptions;
- integration or fix instructions.
