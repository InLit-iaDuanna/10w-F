# 七领域联通：当前测试交接

2026-09-10，当前项目 `/Users/isduanna/123456`。本轮按用户要求停止扩展，交付当前版本等待实测；不标记全部流程验收完成。

七个领域固定为：策划与制作、资产与动画、场景与关卡、玩法与交互、材质与画面、界面与声音、试玩与交付。原六个功能包的 ID、会话、源码关联保留，跨领域引用同一项目对象。七领域中已写入当前真实进展，尚未标为 verified。

## 已接通

- 专业工具入口共用当前项目、工作区、资产版本和实体引用。专业制作配置可以交给原主对话的 Agent；不创建第二个主聊天。
- 环境场景读取真实竞技场实例与五盏全局灯光，场景 v7。修复版本化 GLB 文件访问及天空光方向；编辑灯光后重新构建。
- 竞技场真实 Blender 几何编辑、材质文档编辑、AI PBR 调整和程序化 Shader 回流。当前采用资产 v5，重新打开原生源后保存的 v6 仍是候选，未自动采用。
- 装甲守卫、步枪登记为实体模板。守卫采用资产 v6，18 个节点身份、16 根骨骼与 Idle/Run/Walk/Wave 四段动画身份在实际 Blender 导入导出中保留。运行实例复用模板，材质独立，销毁不释放共享几何。
- Blender 导入时排除实际由骨骼引用的匿名编辑辅助网格；清理隔离候选中旧对象和旧 Action。通过 glTF 导入扩展获得源动画与实际 Action 的映射，不靠名称猜测身份。
- 已验证守卫采用 v6 → v1 → v6；缺少动画身份的 v2 采用请求被拒绝，旧采用版本保留。失败中间候选保留历史，不冒充正常产物。
- 音频工作台保存真实 WAV 与分析结果，波次音效配置接到游戏 AudioFeedback。声音在用户开始／恢复后启用。修复步枪换弹计时结束未补弹。
- 新增原生 Agent 实体读取、规整提案、规整、版本采用能力；保留授权、请求 ID、版本检查及任务占用规则。

## 运行与验证

最新试玩：http://127.0.0.1:59506/ 。构建候选 `game_candidate_7b0cb420d7fe48628590eb03eb8a3082`，任务 `task_project_demo_agent_60f58ad046b44f7c81e043ee52a92b04_native_06a4428dc31141a7a52e0b9e0e86d6ad`。构建采用：竞技场 v5、守卫 v6、步枪 v1、场景 v7。

真实证据：Blender 5.1.2 导入／保存／导出、18 个节点和四段动画 ID 回读；真实材质 AI 调用、原生 Agent 专业文档读取、音频 WAV 分析；实际工程构建。独立 Chromium 显示启动界面，点击开始后可见竞技场、武器和 HUD；截图 `output/playwright/seven-domains-final.png`。

模拟测试：Blender 适配器 38 项通过；相关 Blender 内容、实体及原生桥测试 13 项通过。实际守卫 GLB 和游戏类在模拟战斗中通过波次 1→2、击杀、材质隔离、共享几何生命周期和重置检查；步枪换弹和模拟 AudioContext 检查通过。模拟结果不等于人工玩法或听感验收。

## Limitations

- 原生 AI 音频任务已真实读取专业配置并复制音频，随后因 CodeBuddy 配额耗尽停止。游戏音频接线由开发补完；不得将该 AI 任务标成功。
- 最终独立 Chromium 已显示并运行；内置浏览器曾在加载阶段空白，清理本轮旧预览并重新加载后，已实际显示启动界面和场景。自动点击开始时浏览器拒绝指针锁定，鼠标、射击和听感留待用户直接操作验证。
- 平台 TypeScript 检查仍有已有的 render-ops、Unity、版本协作等错误；此次涉及入口文件未在检查结果中报错，不能据此宣称全库检查通过。
- Unity、外部重定向、音频生成及离线渲染等已有外部适配流程，仍按各自 live/mock/blocked 状态呈现；本轮没有把离线示例改称实测成功。
- 尚未进行全流程人工验收。版本号与构建成功不能替代用户对各工具编辑、更新游戏、鼠标手感、命中反馈与声音的确认。
