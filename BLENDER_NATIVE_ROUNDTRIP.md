# Blender 原生资产编辑往返

本轮接入 Blender → 当前 Web Demo，沿用 D4 的任务、授权、资产版本、实例引用和独立试玩候选。基于本地 `1e90f54`（产品 D4 为 `514648b`）；保留工作区其他未提交修改。Unity、Xcode 未改造。

## 已实现

- 内容列表区分程序化配方、Blender 原生源和其他文件源，提供显式打开与保存同步入口。旧任务需原授权卡明确扩展 Blender 范围，不自动获得权限。
- Agent 默认无头，手工入口使用可见的专属 Blender。沿用认证 loopback、主线程执行、macOS sandbox-exec、目录外拒写探针及自有进程停止；未接入另一套 MCP、任意 Python 或素材服务。
- 类型化 begin 从当前资产版本创建候选；首次把门配方转换为模型，后续复制并重开已登记 `.blend`。edit 按稳定节点 ID 修改几何或基础颜色。源与 GLB 分别保存，发布版本不指向编辑器中的可变文件。
- 门框分成独立柱和过梁，门扇挂在偏置铰链下。导出保留稳定身份和语义；显式保存时为手工新增的未标记对象登记身份，遇到其他资产身份则拒绝，不静默丢弃自由几何。
- `source_kind=blender` 保留父源版本，不再携带可编辑 DoorRecipe；Web 链不要求 FBX。登记检查源版本，引用应用可指定实例子集，保留行为、实例 ID 和变换。引用冲突保留已登记源，源冲突保留不可变候选文件。
- 对象／组件与 ECS 模板使用实际 GLB 加载器、独立铰链旋转和按网格包围盒计算的碰撞。旧生成工程在当前源码授权下先预览，再用版本化三方合并接入加载器；不冲突的自定义代码保留，冲突交给代码编辑任务。
- Blender Technical Artist 技能进入实际 Provider 请求，继续保留原制作、Debug、QA 技能。新授权使用不同内容与状态目录，不能重绑定旧会话。

## 验证与证据

原始证据放在 `.local/blender-roundtrip-evidence/` 和 `output/playwright/`，不作为产品源码提交。

1. **真实 Blender 5.1.2**：可见及无头模式均完成创建、保存、导出、重开和第二次编辑；保留第一轮修改。非对称门、偏置铰链、额外把手身份和授权隔离目录均有实际文件。最新含续授目录回读的记录：`/private/var/folders/my/6wlls8751xgdx2cvhq5d7fvh0000gn/T/sceneops-native-roundtrip-84fzb9kg/evidence.json`。新增把手来自开发者模拟手工动作，不能称为 UI 手工验收。
2. **真实产品模型**：`real-model-20260907-221718/task-result.json`、`summary.json`、`glb-readback.json`。CodeBuddy `glm-5.3-flash` 消耗 11 次请求，自主调用读取、Blender begin/edit/publish、物化、检查、构建、预览与 finish。实际门扇线性 RGBA 为 `[0.05,0.45,0.45,1]`；两个实例身份、变换与 KeyDoor 前后相同。初始化场景是明确的领域夹具，模型修改不是开发者代做。未向模型发送截图，不声称模型看过画面。
3. 成功源为 `blend_f61f8bdc76524d699da05dbdc71b7f67.blend`，资产版本 `aver_blend_f61f8bdc76524d699da05dbdc71b7f67`，实际 GLB 位于同一证据项目 `public/sceneops-assets/`。构建 `game_run_9635d26ed78442ed9358f96a285b1c3d` 成功，候选 `game_candidate_7bf772a7a9a440378cb74864b08be6fb` 保留。
4. 首次真实模型尝试完成原生修改与发布，但发现派生 TS 类型不接受 `export_name: null`。修正后重新实测成功；首轮 16 次请求及取消记录保留。同项目新授权因原执行占用被拒绝，没有绕过。成功运行使用新的独立测试项目。
5. **Shell**：实际选中原生资产，打开编辑候选，从 `.blend` v2 显式保存导出为 v3。测试工程依赖问题导致更新失败时，UI 保留旧候选并显示失败；修复测试依赖后，实际检查和构建成功，从 Shell 显式打开了新版独立试玩页。截图 `output/playwright/blender-shell-save.png`、`blender-game-loaded.png`。实际键盘检查：持钥匙时在 `x=-2.038,z=-3.623` 被关门阻挡并显示开门提示；按空格后门扇旋转、门框留在原位，继续前进到 `z=-5.000`。截图 `blender-door-closed.png` 与 `blender-door-open.png`。初始场景制作使用脚本化模型，后续 Blender 调用是真实工具，不能混称真实模型 Shell 全链。
6. 服务关闭重开后，Shell 恢复读取原生资产与可玩版本 2，旧试玩打开记录标记“当前会话未确认”；测试授权自然到期，继续写入被禁用，未自动续授。
7. 定向协议与领域：现有 8 项会话测试、新增协议、目录隔离与身份测试；原 D4 保存/续授回归；新源登记、坏导出、版本冲突、手工占用拒绝；真实二进制 GLB 夹具的门框、铰链、碰撞和坏模型检查。最终命令输出见 `protocol-final.log`、`domain-final.log`。D4 前端 4 项通过；两种模板通过类型检查与 Vite 构建，只有对象／组件架构具有本轮真实模型制作证据。

## 补完记录（2026-09-08）

此前过早结束，缺少 GUI 手工几何编辑和旧消费者接入。本次已补完这两项：

- 在真实 Blender 5.1.2 界面打开当前产品候选，直接把门扇 Y 缩放设为 1.8，并按保存。产品导出回读确认厚度从 0.117 m 变为 0.2106 m；稳定 leaf ID 保持。截图 `.local/blender-completion/manual-blender-saved.png`。这次是实际 GUI 操作，区别于此前开发者模拟编辑。
- 修复外部窗口保存后后台仍持有旧场景的问题：后台无未保存修改时重新打开登记候选再导出；磁盘和内存同时修改则返回 `BLENDER_SOURCE_CONFLICT`，保留两边。真实隔离 Blender 的刷新与冲突检查也已通过。
- 通过同一产品任务发布资产 v2，两个共享实例的 ID、变换和 KeyDoor 行为不变。将原 D4 的三个旧消费者文件复制到该隔离测试工程，经实际任务 preview → ChangeSet → upgrade 完成三方合并。原 D4 工程与其已过期授权没有被改写或续授。
- 使用实际包管理器完成 TypeScript 检查与 Vite 构建，构建 `game_run_d1b5db7884084104977d65187f1b0146`，候选 `game_candidate_4ac67a9a037a48b1ab52bac222c0104f`。
- 实际键盘试玩：无钥匙被阻挡于 `x=-2.042,z=-3.581`；道具从 0 变为 1；持钥匙时在 `z=-3.586` 出现开门提示；按空格后穿过至 `z=-5.000`。截图 `output/playwright/blender-complete-no-key.png`、`blender-complete-closed.png`、`blender-complete-open.png`，门框固定、门扇独立旋转。浏览器未记录错误。
- 仅本次差异的独立源码树通过 18 项迁移、Blender 修订、续授、内容及项目 Demo 测试；另有 2 项当前任务精确差异审计与过时／外来提案拒写测试通过。迁移覆盖两种架构，自定义内容保留、冲突、符号链接、未来模板变化及并发编辑。

补完证据入口为 `.local/blender-completion/summary.json`；完整任务、资产与构建记录位于其中的 `source_evidence_directory`。本次手工验证初始场景使用显式脚本夹具，前一轮 11 次真实模型请求证据独立保留，不混称同一模型运行。

## 操作

从作品的「选择实际内容」选择共享资产。原任务没有 Blender 权限时，准备并确认原有有限授权卡。点击「用 Blender 编辑」，编辑专属候选；点击「保存 Blender 源并同步导出」，再「更新 Demo」，显式打开新试玩。模型入口使用同一目标和工具。候选源被人工持有时，模型不能停止该窗口后覆盖它。

## Limitations

- 本次浏览器实测覆盖无钥匙拒绝、收集、碰撞和开门穿过；异常验证覆盖源冲突及代码升级冲突等具体场景，不声称穷举所有异常组合。
- 旧生成工程已有版本化迁移；任意外部导入工程或三方合并冲突仍需具体代码编辑。当前生产能力以静态几何与基础材质为主。
- 碰撞使用每个网格的世界包围盒，不是三角网格物理；凹形门框需保持分段网格。没有动画、骨骼或任意模型生成承诺。
- 运行中的 Blender 操作不安全中断；排队取消可处理，状态未知时拒绝盲目写入重试。排队取消状态尚未跨进程持久化。
- 工作区全量 TypeScript 检查仍存在其他模块的错误；本轮新字段调用错误已修复。前端测试恢复了已有本地依赖链接。Corepack 在真实执行中下载 pnpm 11.13.0，Shell 测试的包管理器也准备了测试工程依赖；没有安装或升级 Blender、Unity、SDK 或第三方 MCP。
- 同一工作区存在其他并行开发差异，提交需仅包含本轮 Blender 差异。未推送、合并用户游戏分支或发布。

参考吸收了 [Blender MCP](https://github.com/ahujasid/blender-mcp) 的双向检查、对象与材质工作流；没有复制其任意 Python 接口或安装其服务。本轮技能为针对现有类型化工具编写的产品资源。
