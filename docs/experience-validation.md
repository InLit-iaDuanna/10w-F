# 经验功能验收记录

2026-09-08，在隔离 SQLite 与隔离工作台端口上验收。现有工作台数据未用于写入测试。

- 35 项后端定向检查通过（含六项主题分类、手动修订与旧数据迁移检查），包括种子安装包资源、项目隔离、并发修订、预算与跨天恢复，以及四条请求接入。
- 3 项新版组件检查通过（主题和工具筛选、阅读/编辑切换、分类上限、设置分离、草稿与引用错误）；新增经验组件及测试的定向 TypeScript 检查通过。全仓检查存在其他依赖/测试类型问题，不声明全仓通过。
- 最新生产构建通过。
- 初版实际页面验收：搜索方向锁、选择条目、保存产生 v2、停用 v3、启用 v4、展开来源和修订、恢复 v1 内容生成 v5，全部经过实际 HTTP 接口。桌面与 390px 窄窗口截图已检查。异源预览的一次写入被现有认证拒绝，随后改为来源匹配的隔离工作台完成验收，没有修改认证措施。
- 真实模型：CodeBuddy CLI / kimi-k3-1，初版与加入主题后的两轮隔离验证，共 4 次独立学习调用（每轮 2 次）。隔离合成材料产生共享案例，另一项目召回 v1，用户纠正后合并为 v2 并再次召回。加入主题后模型自动归类为“工程与协作”，修订后分类继续保留；没有额外分类调用。保持未验证，费用未知。这不属于真实游戏或设备行为验证。

## 重设计验收

按用户要求研究 Hermes、Letta、Hindsight 的一手 GitHub 资料，分类和取舍记录在 [组织方案](experience-organization.md)。

新版在完整工作台中实际通过：六个主题及计数、Xcode 工具筛选、方向锁搜索、Markdown 阅读、来源展开、修改主题生成新修订、恢复旧修订、停用/启用、学习动态与设置分离、独立读取开关、390px 窄窗口与返回列表、Escape 关闭。桌面卡片按内容自然排布；详情返回入口随滚动保持可见。

35 项后端定向检查、3 项组件检查、经验组件定向类型检查和最新整站生产构建通过。构建期间其他模块一度处于导出缺失状态，恢复后已重新通过完整构建；没有为通过构建而修改这些模块。隔离组件预览只用于中间排查，最终截图来自完整工作台。

- [新版经验库](../output/playwright/experience-library-v2.png)
- [新版详情](../output/playwright/experience-detail-v2.png)
- [窄窗口](../output/playwright/experience-mobile-v2.png)

Limitations：全仓 TypeScript 检查存在既有依赖/测试类型问题；仅上述新增组件的定向类型检查通过。四条接入路径使用注入测试，真实模型端到端验证在经验服务隔离项目中完成，不等于已实测所有原生工具任务。没有发布、部署或启动 Blender/Unity/真机。
