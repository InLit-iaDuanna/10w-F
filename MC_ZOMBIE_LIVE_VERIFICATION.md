# 方块打僵尸：实际验证与制作交付

## 当前方向（2026-09-06）

按用户最新要求，优先跑通制作流程，暂停扩展 AI 玩游戏。新任务默认不授权 Play、capture、verify；主对话默认提交 `allow_playtest: false`。自动游测不再是制作交付前提。

制作交付仍检查：实际场景版本、原生参数回读、场景与登记产物字节一致、编译状态和控制台。通过后任务为 `review_required`，不标记玩法已验收。历史任务已有的游测授权和证据保留；专用 P0/P1 脚本显式申请游测权限，默认工作流程不调用它们。

## 已执行的真实闭环

- P0：固定受控生存射击组件在真实 Unity Editor 完成 14 项输入、移动、射击、胜负、重开与证据检查，不调用模型。
- P1：`glm-5.3-flash` 根据真实失败调整血量和伤害参数，3 次模型调用、1 轮修复；同一任务的新场景版本完成 14 项检查。未改变场地、敌人数、胜利目标和种子。
- 修复过的启动问题：不存在的 `com.unity.modules.inputlegacy` 包依赖，以及恢复时重复打开同一 Unity 工程。
- 最新变更只运行生产交付合同烟测，不再次自动玩游戏。

P1 任务：`task_859e04b7450b483598dc039fbdc6bf84`。
报告：`.local/mc-zombie-validation/task_859e04b7450b483598dc039fbdc6bf84.p1.json`。
Unity 工程：`.local/v5-preview/agent-workspaces/prj_0388a50614aa45f8b5ea7bb69b10adca/unity`。
场景：`Assets/SceneOpsPrototype.unity`。

## Limitations

这是固定可信配方的参数化制作与修复，不是任意游戏源码生成。原生界面的移动、跳跃和 HUD 已观察，操作系统鼠标射击尚未独立确认。完整游戏体验由用户试玩。

其他生产节点不能据此宣称全部跑通：跨任务受控工程继续编辑、通用角色动画与任意玩法生成仍需实现/验证。没有执行独立生产构建、离线渲染、商业发布或付费资源调用；当前交付是依赖本仓库 Unity 包的可编辑工程。
