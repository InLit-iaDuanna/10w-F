# 暖陶与松影 · 工作台固定资产

工作台现内置 24 项原创可复用资产：3 套完整场景、17 件建筑／物件、4 个同造型异色角色。材质采用统一的暖陶、青绿和木色，全部提供真实 GLB 与渲染缩略图；授权为 CC0-1.0。

| 场景 | 主要内容 | 三角面 |
| --- | --- | ---: |
| 暖陶村落广场 | 小屋、井、石板路、围栏、瞭望塔、树木与角色 | 17,724 |
| 林间营地 | 帐篷、营地道具、林木与开放路径 | 8,905 |
| 荒原驿站 | 废墟、前哨设施、路面和可复用道具 | 15,296 |

三套场景占地约 21 × 18 米。角色使用相同几何造型，以蓝、红、黄、绿区分玩家、敌人、NPC 和友方。单个建筑与道具也分别导出，可以重新组合。

## 工作台使用

从区域功能列表打开「内置场景与资产」，或在「环境场景」的项目资产区点击「内置资产」。选择场景或物件后可查看效果图、旋转真实 3D 模型、下载 GLB，或「加入项目资产库」。

加入项目使用原有真实 Blender 导入流程，生成项目自己的 `.blend`、GLB 和 FBX 版本；原始内置文件不被改写。重复采用同一项会返回已有项目资产，随后可在环境场景中创建多个实例。

确认包含内置素材的 Demo 任务后，同一个已授权、已记录的资产准备动作会把整套 GLB 及目录复制到卡片工作区的 `public/sceneops-assets/sceneops-haven-kit/v1/`。Agent 可通过目录中的 URL、尺寸与出生点使用这些场景；已有用户文件会保留。

原始资产、许可与索引位于 `modules/asset-library/backend/src/asset_library/builtin_assets/`，随 Python 包一起打包。可编辑的生成脚本、来源与核验记录位于 `modules/asset-library/builtin-assets/`。

## 验证

- 24 个 GLB 经 Blender 重新导入核对尺寸、三角面与稳定对象标识；无外部 buffer、相机或灯光依赖。
- 三套场景与代表性物件经过实际渲染检查；出生点及其周边无遮挡。
- 实际工作台中验证功能入口、缩略图、分类和 GLB 三维预览；窄窗口画布尺寸稳定，隐藏编辑器释放渲染器。
- 目录／文件接口测试 2 项、固定资产复制及真实 Blender 项目采用测试 2 项通过。后者验证重复采用不重复建资产，用户文件不被覆盖。
- 构建后的 wheel 包包含目录、24 个 GLB 和 24 张预览图；前端构建、模块校验和生成一致性检查通过。

## Limitations

这是可复用的场景与物件资产，不附带角色控制器、碰撞体或导航网格。Demo 的玩法、碰撞和交互由确认执行后的 Agent 按项目范围实现。缩略图为离线渲染，工作台实时预览的灯光效果有所不同。
