# 统一应用交付与启动烟测

日期：2026-09-05（Asia/Shanghai）。分支：`codex/integrated-workbench`。

## 结果

11 个工作台历史已汇入主项目的整合分支；原工作树、其未提交改动及历史数据未改动。统一 Web/API 已启动，入口为 `http://127.0.0.1:4300`。本轮没有执行完整测试或业务案例。

| 本轮检查 | 结果 |
| --- | --- |
| 根 `pnpm dev` | Vite 8 / Uvicorn 启动，监听 127.0.0.1:4300 / 8300 |
| `GET /api/health` | 200；ready、sqlite、empty、external_execution=not_started |
| `GET /api/workspace/modules` | 200；Shell + 10 个业务分组 |
| `GET /api/workspace/projects` | 200；projects=[]，未初始化项目 |
| 首页 | 对话空态、四边拉手、模型选择可见 |
| 10 组业务编辑器 | 公开加载器及原生空态逐组加载，无 iframe；未选择项目 |
| 本地项目入口 | 空列表、创建按钮显示；未点击创建 |
| `GET /api/ai/models` | 200；本机 CLI 可被定位，默认项 + 15 个候选模型；不证明登录或模型权限 |
| `GET /api/ai/settings` | 初次返回 cli-default |
| `GET /api/ai/conversation` | 初次返回 messages=[] |
| 浏览器控制台 | 空态最终仅剩原有 Dockview Enterprise 未授权水印提示 |

本轮启动发现并修复了空 IntegrationGateway 初始化错误、AI 游测参数重名及项目快捷按钮遮挡停靠标签的问题。修复后继续同一轮启动烟测，没有扩展为业务回归测试。Vite 的开发期依赖预处理不等于生产构建；没有运行生产构建命令。

烟测使用独立浏览器，只操作停靠工具和空态入口。可见浏览器中另观察到手动 AI 请求返回 503；这些请求不是代理发起的烟测操作，代理没有点击发送或重试，也没有更改其模型选择。真实 AI 对话尚未成功验证，CLI/账户错误仍需后续单独定位，不能据模型目录通过声称推理可用。

## 已实现的边界

- 本地 SQLite 保存项目、聊天、模型设置和明确保存的草稿。制作计划、角色版本接 repository；评审和构建沿用原 SQLite，按项目隔离。
- 选择、任务、场景及资产上下文通过公开 props/接口共享；固定项目面板与全局项目分开。最后选中的项目 ID 与布局留在浏览器，草稿不作为整份临时面板状态写库。
- AI 共用 CodeBuddy provider；禁用工具、限制 MCP，提供取消、超时和手动重试。明确选择的已保存模块草稿才会附带给 AI；建议不会自动采用。
- 手动示例入口保留并标为 Mock。启动不导入样例、不规划渲染任务、不启动游测或外部生产。
- 删除的是整合分支中的重复子目录 pnpm 锁文件/配置；根 `pnpm-lock.yaml` 为统一依赖来源。原文件可由 Git 历史或原工作树恢复。

## Limitations / 未验证清单

- 未执行项目创建、保存/恢复、跨项目切换、版本冲突、审批等业务操作；相关实现仅静态检查，需你手动验证。
- 未运行 demo 案例、完整单元/集成/E2E、类型检查、生产构建、安全或性能测试。
- 代理未发起真实 AI 推理；登录、额度、模型权限、取消与超时的真实 CLI 行为未验证。已观察到的手动请求 503 尚未定位。
- Unity、Blender、ComfyUI、渲染、AI playtest、生产发布均未运行或验证。
- 概念资产、构建发布、版本评审的自有生产链仍以规格/评审草稿为主；完整原面板需要已有领域数据或明确导入 Mock。角色/世界/逻辑/UI/音频接入了自有数据编辑入口，但这些业务入口未在本轮运行。
- 概念、渲染、UI/audio/VFX 原服务的交互会话仍是内存状态；只有明确保存的草稿持久化，临时提案/运行缓存不能当作已保存生产记录。
- Dockview Enterprise 当前没有有效许可证，保留原有水印；未修改或绕过许可证机制。
- 旧独立 lab 入口保留但没有逐个启动验证，旧环境和数据未自动迁移。

安装与日常使用见根 `README.md`；服务接口与存储边界见 `services/api/README.md`。
