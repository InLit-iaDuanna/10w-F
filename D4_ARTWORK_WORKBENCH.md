# D4：作品工作台与内容编辑回流

D4 将真实项目内容接入 Shell：用户可以试玩已有作品，选择共享配方、实例或 KeyDoor，保存源内容，更新同一 Demo，并从明确目标向原任务追加修改。新版本就绪不会刷新正在玩的旧页面。默认初版入口仍为 D3 `project-demo-agent`；固定 `project-demo` 明确标记为示例，不作为失败回退。

本轮以已有工作区继续开发，只提交 D4 差异，不提交用户游戏源码，不合并游戏分支，不推送。实现与验证在工作区及隔离副本进行；其余未提交工作保留。

产品提交：`514648b`。验证夹具与本说明作为后续独立提交；完整提交编号在交付回复列出。

## 用户路径

1. 在对话中说明 idea。初版摘要从已有方向、结构化策划或用户原文带入；未知视角、范围、架构留空，需要补齐并确认。详细策划与四卡保留高级入口。
2. 确认当前项目的制作范围和有限预算，开始真实 Agent 制作。有作品后，试玩与继续修改位于方向表单之前。
3. 从“选择实际内容”进入对应编辑器。配方显示受影响的真实实例；实例属性与 KeyDoor 参数分别编辑。
4. “保存源内容”只保存权威源。“更新 Demo”执行物化、检查、构建和预览，不调用模型。构建失败保留上一成功候选，显示本次失败。
5. “让 Agent 修改此内容”携带选中身份、源版本、当前查看候选与修改要求，沿同任务继续。改实例与改共享资产有不同写入范围。
6. 新版就绪后显式打开新的独立试玩页。旧页保持自己的玩家进度。重开恢复作品与草稿；历史打开记录标为“当前会话未确认”，不假称旧窗口仍在运行。

## 权威源与接口

| 内容 | 权威来源与编辑方式 |
|---|---|
| 共享门 | 现有资产库的版本化 DoorRecipe：宽、高、厚度、颜色、粗糙度、金属度；经 `update_recipe` 保存 |
| 实例 | 现有场景服务的稳定对象 ID、资产版本引用、位置、旋转、缩放；经 `transform_object` 保存 |
| KeyDoor | 稳定行为 ID，服务端解析所属实例；经 `update_key_door_behavior` 修改钥匙、距离、角度 |
| 普通源码 | 实际成功且已提交效果的 `code.file.write` 记录建立导航；稳定身份取该路径首条写入记录，最新写入与源码正文独立校验；通过 Agent 编辑 |
| 候选 | 现有构建运行、候选、可玩指针表；每次成功输出单独保留，试玩打开经过归属核对的保留产物 |
| UI 草稿 | 按项目、工作区、目标种类与稳定 ID 保存在浏览器；不是“已保存”的权威源 |

新增小型 Pydantic 接口位于原任务路由：`project-demo/content`（读取/保存）、`project-demo/play`、`project-demo/continuation-authorization`。原追加目标接口增加 `target`。OpenAPI 与前端类型由原生成命令生成，没有通用 IR 或第二套内容数据库。

归属在服务端重新核对，客户端路径不能决定写入路径。选中实例只允许该实例变换，选中行为只允许其参数，选中资产只允许该资产配方及其引用升级，选中源码只允许登记的目标路径；构建等派生操作仍受原授权控制。过期草稿不会静默覆盖新源；共享资产同时校验用户看到的场景引用版本。

资产版本保存与场景引用升级使用各自公开服务的版本检查。若配方已保存而引用升级冲突，明确返回“已保存但未全部应用”，重读后再次保存复用已有配方版本并应用剩余引用，不重复造版本。物化仍使用原 D2 来源，生成几何与碰撞，普通行为源码不会被替换。

候选增加普通文件修订元数据（大小、修改/变更时间），用于发现任务工具以外的源码修改；没有增加内容哈希。没有该元数据的历史候选明确提示无法核对完整源码，不虚构历史来源。它是源变化提示，不是跨文件系统的内容身份证明。

## 继续授权

继续制作沿原任务和登记工作区进行。旧授权到期、耗尽或安全停止后被撤销时，可以申请新的明确有限授权：28 次模型请求、32 个动作、30 分钟。历史授权、动作、目标和模型请求累计数保留；当前窗口只记录起始偏移。确认新范围不会自动重跑旧目标，用户可以继续发送保留的要求或更新已保存源。

存在运行中、效果未知、清理未确认的动作时不能通过续授绕过核查。安全停止且重新授权的同任务在实际排队时重新取得项目执行权；其他项目/任务的锁不会被绕过。旧授权不会因为新增 `environment.asset.rebind` 自动扩大权限。

## 真实 Shell 与模型证据

复用 D3 已真实生成的隔离对象/组件工程 `Real model key door`，复制其数据库到隔离运行目录；原工程已有方向来自 D3 公开 composition 回调，本次复用该记录，并保留正常 Journey 回调处理新项目。未将示例任务伪装成真实模型。

- 项目 `prj_812eade311c54e499ea441533d7728cb`；工作区 `demo_ws_84e4a7e86a424ecdaf082aa0f1b572ff`；原任务 `task_project_demo_agent_55555555555555555555555555555555`。
- 通过真实 Shell 将出口门交互距离 2 → 1 米。独立浏览器收集钥匙后在约 1.5 米处检查：v1 显示开门提示，v2 无提示。另一共享门实例没有被添加/修改行为。
- 通过共享配方面板把厚度 0.15 → 0.35 米，显示两个引用实例，保存为资产 v2，引用均升级；独立“更新 Demo”生成新候选。
- 从真实 KeyDoor 入口发送“仅将该门改为 0.8 米，保留钥匙、角度和其他内容”。真实 `glm-5.3-flash` 读取内容、经公开参数服务保存、物化、类型检查、构建并启动预览。场景 v6，候选 v4。新增 14 次模型请求，原任务累计 18 → 32；CLI 未提供费用/token，不推算平均成本。
- 模型最后因浏览器可执行文件缺失而报告受阻，不能算作完整内置验收通过。独立浏览器在约 0.917 米、已取得钥匙时确认没有开门提示。
- 修正本地浏览器模块配置后，人工调用已有公开观察服务成功：画面加载、截图、页面/控制台错误为空；结果仍为 `gameplay_verified=false`，不把加载检查称为玩法理解。
- 实际注入 TS2322：检查失败，旧候选仍可打开。另在入口源码注入缺失模块导入：Vite 构建候选 v8 失败，可玩指针保持 v7；用户已打开的 v4 页面没有自动刷新。恢复原源码后 v9 成功，显式打开新版。
- 服务关闭重开后仍能读取真实资产、实例、场景 v6 与可玩 v9。未发送目标草稿保留；旧草稿先提示版本冲突，显式重读取得 0.8 米，保留文字要求。640 像素窗口没有页面横向溢出；长实例身份、错误提示与表单焦点经过浏览器检查。
- 另外通过公开服务准备空项目，在真实 Shell 确认短方向并触发初版按钮：服务返回 `project-demo-agent`、空 `card_id`、稳定工作区、0 次模型请求。此项在浏览器测试中明确拦截授权传输，因此只证明真实准备与路由，没有重复付费制作新游戏，也不算第二条真实模型链。

本地原始记录位于 `.local/d4-evidence/`：真实模型任务、最终候选/运行记录、独立观察返回、Shell 截图、重开与入口检查、各层测试输出。记录目录不纳入产品源码提交；临时项目路径仅用于证据，不写入产品构建逻辑。

## 验证命令与结果

临时依赖环境：`SCENEOPS_PYTHON=/private/tmp/sceneops-d4-runtime/bin/python`。共享工作区依赖在验证期间被外部清理，随后使用独立安装目录完成验证，没有把该外部清理提交到本轮。

- 定向服务：`node scripts/python.mjs -m pytest modules/ai-agent-runtime/backend/tests/test_demo_workbench.py modules/ai-agent-runtime/backend/tests/test_demo_continuation.py modules/ai-agent-runtime/backend/tests/test_project_demo_agent.py modules/ai-agent-runtime/backend/tests/test_project_demo_task.py integrations/codebuddy-cli/tests/test_output.py integrations/ai-provider/tests/test_cli_completion.py -q`。工作区 24 通过；仅 D4 差异的审阅副本对应 22 项（现有未提交测试数量不同），其中最终 D4 6 项全部通过。
- D4 服务测试使用脚本化决策和夹具构建，覆盖稳定源码身份、跨项目/候选拒绝、过期源拒绝、目标写入范围、共享版本部分保存恢复、独立保留预览、续授幂等与累计预算。它们不是付费模型验收。
- `pnpm --filter @sceneops/ai-agent-runtime test:d4`：4 项 DOM/目标测试通过；`node --experimental-strip-types --test modules/design-room/frontend/src/tests/initial-direction-draft.test.ts`：3 项摘要测试通过。原执行入口的 3 项组件回归也通过。
- `pnpm exec vite build --config apps/web/vite.config.ts`：工作区与仅 D4 的审阅副本均通过。全工作区 `pnpm exec tsc --noEmit` 仍有既有错误；在相同依赖与命令下对比改动前副本，归一化路径和行号后没有新增诊断，不将全仓类型检查写为通过。
- S1–S3 定向三个文件：`test_game_project_runtime_smoke.py test_browser_observation.py test_browser_interaction.py`，42 通过、12 失败、9 跳过；改动前相同环境与命令得到相同失败集合及数量。失败包括派生测试继承夹具签名/候选数量不匹配，原记录保留。
- 两种架构的模板/演示资产回归 2 项通过；当前工作区 KeyDoor 共享资产升级与重开领域回归 1 项通过。
- 额外真实 S3 键盘回归最终 2 项因 `BROWSER_TIMEOUT` 失败；采用相同隔离依赖夹具的改动前副本也复现同一失败集合和错误码，未计为通过。检查过程中发现测试把共享依赖目录链接进可写工程，包管理器会改写链接并破坏后续运行；夹具改为每个测试复制自己的依赖链接树，保留原失败记录。这不改变产品权限。

## Limitations

- ECS 仍没有本轮真实模型制作覆盖；额外的通用键盘回归不等于本轮 D4 目标行为的全部自动验收。对象/组件的实际 Shell 续改与浏览器观察独立记录。
- 普通行为只提供真实源码/Agent 入口，没有通用参数面板；无可靠来源的文件不虚构组件绑定。旧文件资产可以读取，未增加 Blender 或通用文件资产编辑。
- 当前内容列表优先，没有 3D 点选；没有付费素材服务、新 Agent 框架或整站改版。
- 不承诺任意 idea 自动制作成功，也不将脚本化测试、独立人工浏览器验收和模型自行验收混为一谈。
- 上述全仓既有类型错误与 S1–S3 失败没有被本轮清零。浏览器实测入口准备项明确停在授权之前。
