# 卡片分支中的受控源码开发

## 操作

1. 点击横向卡片，创建或恢复 Git worktree。
2. 主输入框选择「开发 · 先授权」，描述本次实现目标并发送。
3. 审阅所选卡片、分支、目录、策划快照和预算，点击确认后开始。
4. 同一主对话显示文件读取、源码写入、前后差异、失败和停止入口，不弹出每步骤的独立工作台。
5. 结束状态是「源码已写入并回读，待检查；未运行或编译」。下一次任务可以继续同一分支。切回项目仍能查看和停止项目任务。

「讨论 / 修改策划」继续原提案流程，不触发代码写入。

## 执行与权限

复用 `AgentTaskService` 与 Harness，新增 `card-development` profile、`card_id`、`branch` 和三个类型化能力：

- `code.workspace.inspect`：列出有界源码清单。
- `code.file.read`：读取指定 UTF-8 源码。
- `code.file.write`：以精确旧内容为前提，创建或修改源码，并回读登记实际结果。

CodeBuddy、Codex CLI、OAI 共用原受限模型入口。模型输出类型化行动，由应用写文件；不启用 CLI Shell 或其他任意工具。当前实现不执行代码、不安装依赖、不构建、不运行游戏、不修改或合并 Git。

授权绑定已登记项目、卡片、实际 Git 分支与 worktree。每个动作和异常恢复都重新核对登记。最多8次模型请求、20分钟、每动作2次尝试；每文件64KiB、每任务32次写入动作及累计512KiB新内容，费用未知。同项目写入继续串行。

主应用准备任务时经 Design Room 公开接口保存最新卡片/大纲上下文快照；不从其他模块私有表取数据，不把上下文当权限。只读 worktree 查询不创建分支。

## 文件与恢复

拒绝越界、隐藏目录、Git元数据、链接、二进制、依赖锁文件和生成目录。写入使用原有 ChangeSet/授权、模块写入日志、旧内容比较和实际回读；首次请求不能把已存在的相同目标内容冒充本请求的写入。

已确认写入可在中断后只读核对；内容无法确认时保留占用，不盲目重放。已停止且写入结果明确的任务释放占用，允许后续任务继续。差异中的 `COMMITTED` 表示文件结果持久化，不表示 Git 提交。

## 本次验证

7项相关烟测通过：5项确定性源码任务检查、1项真实Git/worktree源码写入、1项只读登记检查。没有调用真实模型、执行生成源码、运行游戏或改变用户项目文件。真实Git烟测确认源码只进入卡片目录、Git HEAD不变，且后续任务可继续。

## Limitations

- 模型实际生成代码的质量、浏览器运行效果和完整游戏尚未实测。
- 这是受控源码生成，不是全权限CLI执行；自动依赖安装、编译、试玩、合并和demo组装仍未开放。
- 旧内容检查、建议锁及原子替换不能保证抵御不遵守锁的外部编辑器在极窄时间窗内并发写入；开发时避免同时手动改同一文件。
- 写入使用独立证据记录，尚未把源码变更接入完整资产预览/版本归档。
- 全仓模块目录生成仍被既有事件schema/manifest问题阻挡，未宣称通过；本次宿主通过公开接口组合，不新增模块依赖。全仓TypeScript仍有历史错误，本次相关文件筛查未发现新增错误。
