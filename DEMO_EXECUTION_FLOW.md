# 制作卡片主动对齐与 Demo 执行

首次进入制作卡片时，Agent 主动提出本轮的第一个对齐问题。已有问题和讨论会保留，重新进入不重复发起。3D 世界卡片也先讨论场景范围，用户仍可主动进入模型制作或环境工具。

回答达到所选对齐深度的上限，或点击「对齐好了 · 整理执行范围」，会形成一个可试玩切片的摘要，并询问是否执行。界面自动准备具体授权卡；不要求再切换 Coding 或重复输入开发目标。

「可以，开始执行」授权当前卡片工作区内的源码修改、依赖准备、类型检查、构建、本地预览和浏览器观察。沿用当前模型与现有 typed-tools 执行机制，不切换提供方；该权限是本项目支持的开发操作范围，不是对整台电脑的无限制 Shell 授权。任务次数、期限和费用说明仍显示在确认卡中。

「先不执行，继续讨论」取消本次请求。新的讨论清除旧摘要，旧授权卡不能用于新的范围。相同摘要反复刷新或并发准备只对应一个任务；已取消的任务不会因刷新自动重新启动。

授权后，第一条有记录的操作准备内置资产。新工程本身也包含资产包：

- `createCharacter(role)`：统一人物造型；玩家蓝色、敌人红色、NPC 黄色、友方绿色。
- `createHouse`、`createTree`、`createCrate`、`createRock`、`createCollectible` 和 `createDemoScenery`。
- 文件为可编辑的 `src/game/sceneops-demo-assets.ts`，使用 Three.js 基础几何体，不需要外部模型文件。
- 支持对象／组件和 Miniplex ECS 两种模板。旧工程仅向已登记卡片 worktree 添加缺失的包文件，不覆盖用户同名文件或替换已有入口。

Agent 完成检查、构建并启动实际预览后，当前卡片的 Demo 自动显示在右侧。失败或源码过期的构建不会当作可用预览显示。预览仍允许新窗口打开。

## 验证

- 制作卡片首次提问／重入、手动收束、对齐上限、摘要身份和范围失效：定向确定性测试通过。
- 准备阶段无写入、授权后安装、保留用户文件、并发重复准备和取消后的复用、旧范围拒绝、普通任务权限不变：6 个定向测试通过。
- 前端 StrictMode 下只准备一次、错误可见且仅点击重试：2 项通过。
- 两种模板真实安装固定版本依赖，`tsc --noEmit && vite build` 均通过；实际浏览器显示 ECS 模板人物与场景。
- 工作台前端构建、32 模块校验和生成一致性检查通过。

## Limitations

本轮未替用户点击真实项目的执行确认，也未消耗真实模型调用完成其射击游戏。验证中的模型回复为明确的固定夹具；资产模板则实际构建并在浏览器呈现。游戏的具体玩法仍由确认后的 Agent 依据当前对齐范围实现。
