# Agent 多平台导出（2026-09-08）

项目菜单、主对话和工具库现在提供「导出」。Android APK、macOS Apple Silicon / Intel ZIP 与 Windows x64 ZIP 共用隔离的 Web 构建，并分别保存进度、日志、尝试次数和下载产物。不依赖 Unity 连接，不启动 Unity 或 Xcode。

## 使用

1. 选择本地游戏项目，打开「导出」，选择目标平台和应用设置。
2. 允许下载本次构建所需的 npm 依赖后开始；系统 SDK、Java 和许可仍由用户配置。已有受阻任务可以补充下载授权，再继续。
3. 页面右侧发送问题，当前提供方通过一次有界模型调用读取本次任务和日志，返回类型化操作。只能调整应用名称、方向及继续／取消，不能运行模型提供的命令或扩大下载权限。
4. 游戏源码、触控和玩法修改通过「交给开发对话确认」带上任务与来源版本进入原有对话；用户可以加入草稿，不会覆盖已有输入或自动发起开发。
5. 普通重试使用原始隔离源码。源码修复后使用「用当前项目重新导出」建立关联的新任务，保留对话；旧日志、产物与验证不冒充新版本结果。
6. 安装包生成与设备验证分别记录。设备验证需填写设备、测试说明，并绑定当前成功的执行轮次。

## 实现

`build-release` 持有 SQLite 导出记录、SSE、下载接口与本地构建适配器；`ai-agent-runtime.ExportAgent` 复用当前提供方并返回可审计的输入、结构化结果和用量。`services/api` 负责组合，与原有本地来源和令牌保护共用边界。

源文件复制前后比较文件身份、大小、修改时间及 Git HEAD，若复制期间保存变化则拒绝混合快照。任务目录不位于源工程内部；排除凭据、缓存、Git 元数据，拒绝资源符号链接。模型无任意路径或 shell 输入，npm 安装脚本关闭，Vite / PostCSS 使用自有配置，Gradle 用户目录隔离。Electron 保持 sandbox、contextIsolation、webSecurity，关闭 Node 集成；通过本地 game 协议加载包内资源。

公开接口：`/api/projects/{project_id}/exports` 的创建、列表、详情、消息、SSE，以及平台继续、取消、验证、产物下载、下载授权更新和刷新源码。后端 Pydantic / OpenAPI 生成前端类型，运行 `pnpm generate:exports` 更新。

默认产物用于本地试玩：安卓调试签名 APK、桌面 ZIP。没有商店发布、正式发行签名、公证、自动更新或系统安全绕过。后端可通过 `SCENEOPS_BUILD_RELEASE_ENABLED=false` 关闭导出 API。

## 验证证据

- 后端定向测试覆盖分平台失败和重试、共享 Web 构建、取消、重启恢复、跨项目下载隔离、设备验证、Agent 范围、下载授权、日志脱敏、并发保存与路径逃逸。
- Agent 定向测试 4 项通过：一次调用、上下文脱敏、禁止任意命令、禁止未选平台和提供方切换。
- 实际应用组合 HTTP 烟测通过，包括本地安全中间件、创建、下载、继续和跨项目隔离。
- 前端组件、请求传输和公开入口烟测通过；导出组件局部严格类型检查通过。
- 独立测试工作区浏览器验证了菜单进入、无 Unity 可打开、三平台受阻提示、刷新后恢复任务及开发对话交接。
- 真实最小 Web 工程构建通过；真实 macOS arm64 ZIP 构建并启动通过。在无开发服务器时运行 JS，中文及带空格的根路径 SVG 成功显示，渲染端 require 不可用。恶意 PostCSS 配置 canary 未执行。

## Limitations

真实 Mac 验证使用最小测试工程，不代表用户完整游戏已通过可玩性验收。Android SDK 尚未配置，Android、Windows、macOS Intel 构建和对应设备交互未执行。真实模型对话未调用，模型接入通过注入提供方验证。联网游戏仍依赖其原有服务。

全仓库 TypeScript 检查仍报告已有其他模块的依赖与类型错误，本次修改的 Shell、PlanningJourney 和导出文件未出现在错误列表。完整模块目录检查仍被其他模块已有导入边界问题阻断；清单通过现有生成器的独立清单校验模式生成，没有修改或移除导入规则。

## 后续提示词与技能增量

按用户后续要求新增环境补齐、安卓、桌面和正式发布四个 Skill 及 Agent 系统提示词，支持按平台实际加载与审计；原生电脑执行与结构化对话的能力区别见 [导出 Agent 技能](EXPORT_AGENT_SKILLS.md)。本轮未安装工具或发起发布。

## 原生执行已接通

后续用户要求已落实：导出页默认原生 Agent，可实际执行本机命令与补齐导出环境；固定构建保留为可选路径。见 [真实命令验证及接线](NATIVE_EXPORT_AGENT.md)。
