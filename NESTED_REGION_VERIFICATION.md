# 层级功能区域与生产流程树（2026-09-05）

本次用户明确要求在现有区域内部继续拆分，而不是在整个页面外围增加全局抽屉。本说明取代统一应用之前的全局 edgeGroups 交互说明；独立 Shell lab 保留旧接口兼容。

## 实现

- 每个可见编辑器都有本区域四边拉手。起始区域的 instanceId 是拆分目标，调用已有 split / referencePanel 接口，由 Dockview 原生树计算几何。新区域为功能选择器，原区域保留内容且可从标题选择功能。
- 双击/键盘均分；指针拖动按目标区域边界计算尺寸。同一拉手向相邻区域反向拖动 48px 后关闭被指向的相邻区域；原生分界线拖回极限也可关闭。两条路径都保留未保存/锁定检查。
- 旧边栏使用公开 group.moveTo 转成普通 grid group，再移除空 edge 槽，保留所有面板 ID 与标签。保存的尺寸尽可能按原生可用空间保留，不保证旧全局布局逐像素不变。浮动/弹出窗口不迁移；不隐藏或修改许可证水印。
- 统一中性灰 tokens：画布 #1b1b1b、面板 #222/#292929、边框 #414141；工具库、标题栏、菜单和诊断沿用聊天配色，错误/警告语义色保留。
- 工具库为九阶段游戏生产流程树及项目起点/命令运维辅助分支。搜索保留父分支和原阶段编号，点击仍原位切换，不自动执行业务。

## 验证

- 模块定向测试 49/49，桥接测试 36/36。
- 当前已安装 Dockview + JSDOM：无中心 grid 的 left/top/bottom 旧边栏共六个标签，迁移后全部保留，三个非空 grid group，零 edge group。
- 4302 隔离预览、独立临时数据目录：旧上下布局转换后两区域边界连续，无空中心带。
- 鼠标把上方区域拆为左右，再在右侧上下细分；左侧和下方原区域尺寸/位置不变。内层分界线拖到顶端，只有对应小区域关闭。
- 搜索 Unity 显示原编号 7；命令搜索节点原位替换工具库，instanceId 不变。未调用真实 AI、Unity、Blender、构建或案例。

## Limitations

全项目类型债未在本次清理。多显示器、弹出窗口、所有脏内容组合及超大布局压力未完整验证；不能声称所有布局路径均已验收。旧全局边栏迁移会改变其空间分配，但不删除其标签和业务内容。

## 真实工具接入待办（只读核实）

本机发现 Blender 5.1.2 与 Unity Editor 2022.3.62f3c1；未发现打开的 Editor 工程。安装存在不等于应用 connected。

当前 integration-center 组合 empty/unregistered gateway；UnityWorkbench 的默认适配器未指定 Editor；Blender LiveAdapter 未在统一应用注册。Blender 默认进程桥禁止打开项目 blend（需要真实文件系统隔离），Unity batch runner 需要目标工程、匹配版本和 UPM 包。当前可用工具也没有 Unity/Blender MCP 连接。

待用户确认目标 Unity 工程及 Blender 文件后：Agent 自动发现匹配安装、执行只读探测、生成接入变更与真实能力报告；必要包安装/工程写入保持审批，业务执行继续沿用类型化白名单和 ChangeSet。不得把 Mock/计划预览标成真实连接，也不把禁用安全检查当作接入实现。
